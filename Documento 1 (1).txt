
--// SERVICES
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")

--// LOCAL PLAYER & CHARACTER
local plr = Players.LocalPlayer
local character = plr.Character or plr.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

--// UPDATE CHARACTER WHEN RESPAWNED
plr.CharacterAdded:Connect(function(char)
character = char
humanoid = char:WaitForChild("Humanoid")
end)



--// LIB
local redzlib = loadstring(game:HttpGet("https://pastefy.app/7JZV3yf6/raw"))()

--// WINDOW
local Window = redzlib:MakeWindow({
    Title = "SPECTRA HUB - Brookhaven RP",
    SubTitle = "by assure.",
    SaveFolder = "testando"
})

--// MINIMIZE BUTTON
local MinimizeBtn = Window:AddMinimizeButton({
    Button = { 
        Image = "rbxassetid://8518491816682", 
        BackgroundTransparency = 0, 
        Size = UDim2.new(0, 40, 0, 40), 
        ScaleType = Enum.ScaleType.Fit
    },
    Corner = { 
        CornerRadius = UDim.new(0, 12) -- 35,1 é meio estranho, pode dar bug visual
    },
})





--=============================================================================
-- TABS
--=============================================================================



local Tab1 = Window:MakeTab({"créditos", "info"})
local Tab2 = Window:MakeTab({"funções", "fun"})
local Tab3 = Window:MakeTab({"Avatar", "shirt"})
local Tab4 = Window:MakeTab({"House", "Home"})
local Tab5 = Window:MakeTab({"Car", "Car"})
local Tab6 = Window:MakeTab({"Troll", "skull"})
local Tab7 = Window:MakeTab({"Music All", "radio"})    
local Tab8 = Window:MakeTab({"Music", "music4"})  
local Tab9 = Window:MakeTab({"RGB", "brush"})
local Tab10 = Window:MakeTab({"Teleportes", "map-pin"})
local Tab11 = Window:MakeTab({"Scripts", "terminal"})
local Tab12 = Window:MakeTab({"ProteÃ§Ãµes", "shieldcheck"})




--=============================================================================
-- funções
--=============================================================================




--=============================================================================
-- TAB: créditos
--=============================================================================



--===============================
-- SEÃ‡ÃƒO: CRÃ‰DITOS DO HUB
--===============================
Tab1:AddSection({ "Crédits do Hub" })

Tab1:AddDiscordInvite({
    Name = "Spectra Hub",
    Description = "Comunidade oficial do Hub",
    Logo = "rbxassetid://107533266955045",
    Invite = "https://discord.gg/EBU4JVVeE",
})

Tab1:AddButton({
    Name = "Info Update",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/assure157tv157157157-boop/Spectra-HUB/refs/heads/main/Spectra%20info"))()
    end
})

--===============================
-- DETECTOR DE EXECUTOR
--===============================
local function detectExecutor()
    if identifyexecutor then
        return identifyexecutor()
    elseif syn then
        return "Synapse X"
    elseif KRNL_LOADED then
        return "KRNL"
    elseif is_sirhurt_closure then
        return "SirHurt"
    elseif pebc_execute then
        return "ProtoSmasher"
    elseif getexecutorname then
        return getexecutorname()
    else
        return "Executor Desconhecido"
    end
end

local executorName = detectExecutor()

Tab1:AddParagraph({
    "Executor",
    executorName
})

Tab1:AddParagraph({
    "Jogador",
    game:GetService("Players").LocalPlayer.Name
})


--===============================
-- SEÃ‡ÃƒO: CRIADORES
--===============================
Tab1:AddSection({ "devs" })

Tab1:AddParagraph({
    "Desenvolvedor Principal",
    "assure_TV"
})

Tab1:AddParagraph({
    "Ajudantes",
    "denolk_new,"
})

Tab1:AddParagraph({
    "Projeto",
    "Spectra Hub"
})

Tab1:AddParagraph({
    "Contas Roblox do assure_TV",
    "udydydi2usy6d6d"
})

--===============================
-- SEÃ‡ÃƒO: MÃDIA
--===============================
Tab1:AddSection({ "midia" })

Tab1:AddButton({
    Name = "Copiar Canal do YouTube do assure",
    Callback = function()
        if setclipboard then
            -- Copia o nome e o link juntos (um abaixo do outro)
            setclipboard("assure_TV\nhttps://www.youtube.com/@assure_TV")
        end
    end
})





--=============================================================================
-- TAB: player
--=============================================================================



--=============================================================================
-- STATUS
--=============================================================================
Tab2:AddSection({"Status do Player"})

-- SPEED
Tab2:AddSlider({
    Name = "Speed",
    Min = 1,
    Max = 2000,
    Default = 16,
    Callback = function(Value)
        if humanoid then
            humanoid.WalkSpeed = Value
        end
    end
})

-- JUMP
Tab2:AddSlider({
    Name = "jump",
    Min = 1,
    Max = 2000,
    Default = 50,
    Callback = function(Value)
        if humanoid then
            humanoid.JumpPower = Value
        end
    end
})

-- GRAVIDADE
Tab2:AddSlider({
    Name = "Gravity",
    Min = 1,
    Max = 2000,
    Default = 196,
    Callback = function(Value)
        Workspace.Gravity = Value
    end
})

-- RESET
Tab2:AddButton({
    Name = "Resetar Status",
    Callback = function()
        if humanoid then
            humanoid.WalkSpeed = 16
            humanoid.JumpPower = 50
        end
        Workspace.Gravity = 196
    end
})

--=============================================================================
-- MOVIMENTO
--=============================================================================
Tab2:AddSection({"Movimento"})

-- INFINITY JUMP
local infJump = false

Tab2:AddToggle({
    Name = "Infinity Jump",
    Default = false,
    Callback = function(v)
        infJump = v
    end
})

game:GetService("UserInputService").JumpRequest:Connect(function()
    if infJump and humanoid then
        humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

-- NOCLIP
local noclip = false

Tab2:AddToggle({
    Name = "Noclip",
    Default = false,
    Callback = function(v)
        noclip = v
    end
})

RunService.Stepped:Connect(function()
    if noclip and character then
        for _,v in pairs(character:GetDescendants()) do
            if v:IsA("BasePart") then
                v.CanCollide = false
            end
        end
    end
end)




--=============================================================================
-- SPIN
--=============================================================================
Tab2:AddSection({"Spin"})

local spin = false
local spinSpeed = 50

Tab2:AddSlider({
    Name = "speed Spin",
    Min = 1,
    Max = 5000,
    Default = 50,
    Callback = function(v)
        spinSpeed = v
    end
})

Tab2:AddToggle({
    Name = "Spin",
    Default = false,
    Callback = function(v)
        spin = v
    end
})

RunService.RenderStepped:Connect(function()
    if spin and character and character:FindFirstChild("HumanoidRootPart") then
        character.HumanoidRootPart.CFrame =
        character.HumanoidRootPart.CFrame *
        CFrame.Angles(0, math.rad(spinSpeed), 0)
    end
end)





-- ===============================
-- ESP
-- ===============================
Tab2:AddSection({ "ESP" })

_G.ESPData = {
    espEnabled = false,
    espType = "Nome + Idade",
    selectedColor = "RGB",
    billboardGuis = {},
    highlights = {},
    lines = {},
    connections = {}
}

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera

-- ===============================
-- COR
-- ===============================
local function getESPColor()

    if _G.ESPData.selectedColor == "RGB" then
        local h = (tick() % 5) / 5
        return Color3.fromHSV(h,1,1)
    end

    local colors = {
        Branco = Color3.fromRGB(255,255,255),
        Preto = Color3.fromRGB(0,0,0),
        Vermelho = Color3.fromRGB(255,0,0),
        Verde = Color3.fromRGB(0,255,0),
        Azul = Color3.fromRGB(0,170,255),
        Amarelo = Color3.fromRGB(255,255,0),
        Rosa = Color3.fromRGB(255,105,180),
        Roxo = Color3.fromRGB(128,0,128)
    }

    return colors[_G.ESPData.selectedColor] or Color3.new(1,1,1)

end

-- ===============================
-- LIMPAR ESP
-- ===============================
_G.clearAllESP = function()

    for _,v in pairs(_G.ESPData.billboardGuis) do
        pcall(function() v:Destroy() end)
    end

    for _,v in pairs(_G.ESPData.highlights) do
        pcall(function() v:Destroy() end)
    end

    for _,v in pairs(_G.ESPData.lines) do
        pcall(function() v:Remove() end)
    end

    for _,v in pairs(_G.ESPData.connections) do
        pcall(function() v:Disconnect() end)
    end

    _G.ESPData.billboardGuis = {}
    _G.ESPData.highlights = {}
    _G.ESPData.lines = {}
    _G.ESPData.connections = {}

end

-- ===============================
-- NAME ESP
-- ===============================
local function createNameESP(player)

    if player == Players.LocalPlayer then return end
    if not player.Character then return end
    if not player.Character:FindFirstChild("Head") then return end

    local gui = Instance.new("BillboardGui")
    gui.Size = UDim2.new(0,200,0,50)
    gui.StudsOffset = Vector3.new(0,3,0)
    gui.AlwaysOnTop = true
    gui.Parent = player.Character.Head

    local txt = Instance.new("TextLabel")
    txt.Size = UDim2.new(1,0,1,0)
    txt.BackgroundTransparency = 1
    txt.TextStrokeTransparency = 0.5
    txt.Font = Enum.Font.SourceSansBold
    txt.TextSize = 14
    txt.Text = player.Name.." | "..player.AccountAge.." dias"
    txt.TextColor3 = getESPColor()
    txt.Parent = gui

    _G.ESPData.billboardGuis[player] = gui

end

-- ===============================
-- HIGHLIGHT ESP
-- ===============================
local function createHighlightESP(player)

    if player == Players.LocalPlayer then return end
    if not player.Character then return end

    local h = Instance.new("Highlight")
    h.FillColor = getESPColor()
    h.FillTransparency = 0.5
    h.OutlineTransparency = 0
    h.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    h.Parent = player.Character

    _G.ESPData.highlights[player] = h

end

-- ===============================
-- LINE ESP
-- ===============================
local function createLineESP(player)

    if player == Players.LocalPlayer then return end

    local line = Drawing.new("Line")
    line.Thickness = 2
    line.Transparency = 1
    line.Visible = false

    _G.ESPData.lines[player] = line

end

-- ===============================
-- APPLY ESP
-- ===============================
_G.applyCurrentESP = function()

    _G.clearAllESP()

    if not _G.ESPData.espEnabled then return end

    for _,p in pairs(Players:GetPlayers()) do

        if _G.ESPData.espType == "Nome + Idade" then
            createNameESP(p)

        elseif _G.ESPData.espType == "Corpo (Highlight)" then
            createHighlightESP(p)

        elseif _G.ESPData.espType == "Linhas" then
            createLineESP(p)
        end

    end

    table.insert(_G.ESPData.connections,

        RunService.RenderStepped:Connect(function()

            if not _G.ESPData.espEnabled then return end

            local color = getESPColor()

            if _G.ESPData.espType == "Linhas" then

                for p,line in pairs(_G.ESPData.lines) do

                    if p.Character and p.Character:FindFirstChild("HumanoidRootPart") then

                        local pos,onScreen =
                        Camera:WorldToViewportPoint(p.Character.HumanoidRootPart.Position)

                        line.Visible = onScreen

                        if onScreen then
                            line.From = Vector2.new(Camera.ViewportSize.X/2,Camera.ViewportSize.Y)
                            line.To = Vector2.new(pos.X,pos.Y)
                            line.Color = color
                        end

                    else
                        line.Visible = false
                    end

                end

            end

        end)

    )

end




-- ===============================
-- UI
-- ===============================

Tab2:AddDropdown({
    Name = "Tipo de ESP",
    Options = {"Nome + Idade","Corpo (Highlight)","Linhas"},
    Default = 1,
    Callback = function(v)

        _G.ESPData.espType = v

        if _G.ESPData.espEnabled then
            _G.applyCurrentESP()
        end

    end
})

Tab2:AddDropdown({
    Name = "Cor do ESP",
    Options = {"RGB","Branco","Preto","Vermelho","Verde","Azul","Amarelo","Rosa","Roxo"},
    Default = 1,
    Callback = function(v)

        _G.ESPData.selectedColor = v

    end
})

Tab2:AddToggle({
    Name = "ESP Ativado",
    Default = false,
    Callback = function(v)

        _G.ESPData.espEnabled = v

        if v then
            _G.applyCurrentESP()
        else
            _G.clearAllESP()
        end

    end
})




--=============================================================================
-- TAB: avatar 
--=============================================================================





--// ===============================
--// ABA SKINS - COPY AVATAR REAL
--// ===============================

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Remotes = ReplicatedStorage:WaitForChild("Remotes")
local plr = Players.LocalPlayer


Tab3:AddSection({"Copy avatar"})

-- ===============================
-- DROPDOWN (MESMO SISTEMA DO TROLL)
-- ===============================

local selectedSkinPlayer = nil
local skinDropdown

local function getPlayersSkins()
    local t = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= plr then
            table.insert(t, p.Name)
        end
    end
    return t
end

skinDropdown = Tab3:AddDropdown({
    Name = "Escolher Player",
    Options = {},
    Callback = function(v)
        selectedSkinPlayer = v
    end
})

local function refreshSkinDropdown()
    local players = getPlayersSkins()
    skinDropdown:Set(players)

    if selectedSkinPlayer and not Players:FindFirstChild(selectedSkinPlayer) then
        selectedSkinPlayer = nil
        skinDropdown:Set("")
    end
end

refreshSkinDropdown()

Tab3:AddButton({
    Name = "Atualizar Lista",
    Callback = refreshSkinDropdown
})

-- ===============================
-- BOTÃƒO COPY AVATAR
-- ===============================

Tab3:AddButton({
    Name = "Copiar Avatar",
    Callback = function()

        if not selectedSkinPlayer then return end

        local LP = Players.LocalPlayer
        local LChar = LP.Character
        local TPlayer = Players:FindFirstChild(selectedSkinPlayer)

        if not (LChar and TPlayer and TPlayer.Character) then return end

        local LHumanoid = LChar:FindFirstChildOfClass("Humanoid")
        local THumanoid = TPlayer.Character:FindFirstChildOfClass("Humanoid")
        if not (LHumanoid and THumanoid) then return end

        -- RESET TOTAL DO LOCALPLAYER
        local LDesc = LHumanoid:GetAppliedDescription()

        -- REMOVER ACESSÃ“RIOS
        for _, acc in ipairs(LDesc:GetAccessories(true)) do
            if acc.AssetId and tonumber(acc.AssetId) then
                Remotes.Wear:InvokeServer(tonumber(acc.AssetId))
                task.wait(0.2)
            end
        end

        -- REMOVER ROUPAS
        if tonumber(LDesc.Shirt) then
            Remotes.Wear:InvokeServer(tonumber(LDesc.Shirt))
            task.wait(0.2)
        end

        if tonumber(LDesc.Pants) then
            Remotes.Wear:InvokeServer(tonumber(LDesc.Pants))
            task.wait(0.2)
        end

        -- REMOVER FACE
        if tonumber(LDesc.Face) then
            Remotes.Wear:InvokeServer(tonumber(LDesc.Face))
            task.wait(0.2)
        end

        -- COPIAR DO PLAYER ALVO
        local PDesc = THumanoid:GetAppliedDescription()

        -- PARTES DO CORPO
        local argsBody = {
            [1] = {
                [1] = PDesc.Torso,
                [2] = PDesc.RightArm,
                [3] = PDesc.LeftArm,
                [4] = PDesc.RightLeg,
                [5] = PDesc.LeftLeg,
                [6] = PDesc.Head
            }
        }

        Remotes.ChangeCharacterBody:InvokeServer(unpack(argsBody))
        task.wait(0.5)

        -- ROUPAS
        if tonumber(PDesc.Shirt) then
            Remotes.Wear:InvokeServer(tonumber(PDesc.Shirt))
            task.wait(0.3)
        end

        if tonumber(PDesc.Pants) then
            Remotes.Wear:InvokeServer(tonumber(PDesc.Pants))
            task.wait(0.3)
        end

        -- FACE
        if tonumber(PDesc.Face) then
            Remotes.Wear:InvokeServer(tonumber(PDesc.Face))
            task.wait(0.3)
        end

        -- ACESSÃ“RIOS
        for _, v in ipairs(PDesc:GetAccessories(true)) do
            if v.AssetId and tonumber(v.AssetId) then
                Remotes.Wear:InvokeServer(tonumber(v.AssetId))
                task.wait(0.3)
            end
        end

        -- COR DO CORPO
        local SkinColor = TPlayer.Character:FindFirstChild("Body Colors")
        if SkinColor then
            Remotes.ChangeBodyColor:FireServer(tostring(SkinColor.HeadColor))
            task.wait(0.3)
        end

        -- ANIMAÃ‡ÃƒO IDLE
        if tonumber(PDesc.IdleAnimation) then
            Remotes.Wear:InvokeServer(tonumber(PDesc.IdleAnimation))
            task.wait(0.3)
        end

    end
})


Tab3:AddSection({"ver ids da skin do player"})


-- ===============================
-- VISUALIZAR SKIN DO PLAYER (MELHORADO)
-- ===============================

local skinItemsDropdown
local skinItems = {}
local animationIds = {}

skinItemsDropdown = Tab3:AddDropdown({
    Name = "Skin Items",
    Options = {},
    Callback = function(v)

        -- SE FOR ANIMAÇÃO
        if v == "Animations (Equip All)" then
            for _, id in ipairs(animationIds) do
                Remotes.Wear:InvokeServer(id)
            end
            return
        end

        -- ITEM NORMAL
        local id = tonumber(v:match("%d+"))
        if id then
            Remotes.Wear:InvokeServer(id)
        end
    end
})

Tab3:AddButton({
    Name = "View Player Skin",
    Callback = function()

        if not selectedSkinPlayer then return end

        local TPlayer = Players:FindFirstChild(selectedSkinPlayer)
        if not (TPlayer and TPlayer.Character) then return end

        local THumanoid = TPlayer.Character:FindFirstChildOfClass("Humanoid")
        if not THumanoid then return end

        local PDesc = THumanoid:GetAppliedDescription()

        skinItems = {}
        animationIds = {}

        -- ===============================
        -- BODY PARTS
        -- ===============================
        if tonumber(PDesc.Head) then
            table.insert(skinItems, "Head: "..PDesc.Head)
        end

        if tonumber(PDesc.Torso) then
            table.insert(skinItems, "Torso: "..PDesc.Torso)
        end

        if tonumber(PDesc.LeftArm) then
            table.insert(skinItems, "Left Arm: "..PDesc.LeftArm)
        end

        if tonumber(PDesc.RightArm) then
            table.insert(skinItems, "Right Arm: "..PDesc.RightArm)
        end

        if tonumber(PDesc.LeftLeg) then
            table.insert(skinItems, "Left Leg: "..PDesc.LeftLeg)
        end

        if tonumber(PDesc.RightLeg) then
            table.insert(skinItems, "Right Leg: "..PDesc.RightLeg)
        end

        -- ===============================
        -- CLOTHING
        -- ===============================
        if tonumber(PDesc.Shirt) then
            table.insert(skinItems, "Shirt: "..PDesc.Shirt)
        end

        if tonumber(PDesc.Pants) then
            table.insert(skinItems, "Pants: "..PDesc.Pants)
        end

        -- ===============================
        -- FACE
        -- ===============================
        if tonumber(PDesc.Face) then
            table.insert(skinItems, "Face: "..PDesc.Face)
        end

        -- ===============================
        -- ANIMATIONS
        -- ===============================
        local animations = {
            PDesc.IdleAnimation,
            PDesc.WalkAnimation,
            PDesc.RunAnimation,
            PDesc.JumpAnimation,
            PDesc.FallAnimation,
            PDesc.ClimbAnimation,
            PDesc.SwimAnimation
        }

        for _, anim in ipairs(animations) do
            if tonumber(anim) then
                table.insert(animationIds, anim)
            end
        end

        if #animationIds > 0 then
            table.insert(skinItems, "Animations (Equip All)")
        end

        -- ===============================
        -- ACCESSORIES
        -- ===============================
        for _, v in ipairs(PDesc:GetAccessories(true)) do
            if v.AssetId and tonumber(v.AssetId) then
                table.insert(skinItems, "Accessory: "..v.AssetId)
            end
        end

        -- ===============================
        -- UPDATE DROPDOWN
        -- ===============================
        skinItemsDropdown:Set(skinItems)

    end
})


Tab3:AddSection({"Trocar Pernas"})

--==================================================
-- FUNÃ‡ÃƒO BASE (NÃƒO MEXER)
--==================================================
local function ApplyLeg(rightLegId, leftLegId)

    local player = Players.LocalPlayer
    local humanoid = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return end

    local desc = humanoid:GetAppliedDescription()

    Remotes.ChangeCharacterBody:InvokeServer({
        desc.Torso,
        desc.RightArm,
        desc.LeftArm,
        rightLegId or desc.RightLeg,
        leftLegId or desc.LeftLeg,
        0
    })

end

--==================================================
-- LISTA DE PERNAS (FÃCIL DE EDITAR)
--==================================================
local LEGS = {
    { name = "Zumbi",       id = 37754710,        type = "Right" },
    { name = "Gelo",        id = 139572789,       type = "Left" },
    { name = "Pau",         id = 14498289771,     type = "Right" },
    { name = "Osso Preto",  id = 14547141130,     type = "Right" },
    { name = "Osso",        id = 14681538960,     type = "Right" },
    { name = "RGB",         id = 14533417546,     type = "Right" },
    { name = "Fina",        id = 79274727445827,  type = "Right" },
    { name = "invisível",   id = 14532583517,     type = "Right" },
}

--==================================================
-- CRIAR LISTA AUTOMÃTICA PARA DROPDOWN
--==================================================
local LegNames = {}

for _, leg in ipairs(LEGS) do
    table.insert(LegNames, leg.name)
end

--==================================================
-- DROPDOWN INTELIGENTE
--==================================================
Tab3:AddDropdown({
    Name = "Escolher Perna",
    Options = LegNames,
    Callback = function(selectedName)

        for _, leg in ipairs(LEGS) do
            if leg.name == selectedName then

                if leg.type == "Right" then
                    ApplyLeg(leg.id, nil)

                elseif leg.type == "Left" then
                    ApplyLeg(nil, leg.id)

                end

                break
            end
        end

    end
})





Tab3:AddSection("Effects Avatar")

local ApplyEmmiter = Remotes:WaitForChild("ApplyEmmiter")

--==================================================
-- FUNÃ‡ÃƒO BASE
--==================================================
local function CreateEffectDropdown(title, effectTable)

    local options = {}

    for _, effect in ipairs(effectTable) do
        table.insert(options, effect.name)
    end

    Tab3:AddDropdown({
        Name = title,
        Options = options,
        Callback = function(selected)

            for _, effect in ipairs(effectTable) do
                if effect.name == selected then
                    ApplyEmmiter:InvokeServer(effect.id, effect.code)
                    break
                end
            end

        end
    })

end

--==================================================
-- FOGO
--==================================================
CreateEffectDropdown("Fogo", {
    {name = "Branco",  id = "18637074370", code = "030FireWhite"},
    {name = "Laranja", id = "18637025451", code = "031FireOrange"},
    {name = "Verde",   id = "18637078598", code = "032FireGreen"},
    {name = "Azul",    id = "18637076370", code = "033FireBlue"},
    {name = "Roxo",    id = "18637070174", code = "034FirePurple"},
    {name = "Preto",   id = "18637072603", code = "035FireBlack"},
})

--==================================================
-- CORAÃ‡Ã•ES
--==================================================
CreateEffectDropdown("CoraÃ§Ãµes", {
    {name = "Amarelo", id = "18635672195", code = "001HeartsYellow"},
    {name = "Verde",   id = "18635727693", code = "002HeartsGreen"},
    {name = "Azul",    id = "18635732186", code = "003HeartsBlue"},
    {name = "Roxo",    id = "18635723426", code = "004HeartsPurple"},
    {name = "Rosa",    id = "18635726250", code = "005HeartsPink"},
    {name = "Vermelho",id = "18635729673", code = "006HeartsRed"},
})

--==================================================
-- ESTRELA
--==================================================
CreateEffectDropdown("Estrela", {
    {name = "Branca",  id = "18637942956", code = "090StarWhite"},
    {name = "Laranja", id = "18637946172", code = "091StarOrange"},
    {name = "Verde",   id = "18637934500", code = "092StarGreen"},
    {name = "Azul",    id = "18637940338", code = "093StarBlue"},
    {name = "Roxa",    id = "18637944796", code = "094StarPurple"},
    {name = "Rosa",    id = "18637947820", code = "095StarPink"},
    {name = "Vermelha",id = "18637949457", code = "096StarRed"},
})

--==================================================
-- FUMAÃ‡A
--==================================================
CreateEffectDropdown("FumaÃ§a", {
    {name = "Branca",  id = "18637791748", code = "080SmokeWhite"},
    {name = "Amarela", id = "18637800482", code = "081SmokeYellow"},
    {name = "Laranja", id = "18637793920", code = "082SmokeOrange"},
    {name = "Verde",   id = "18637789299", code = "083SmokeGreen"},
    {name = "Azul",    id = "18637803021", code = "084SmokeBlue"},
    {name = "Roxa",    id = "18637813360", code = "085SmokePurple"},
    {name = "Vermelha",id = "18637796598", code = "086SmokeRed"},
    {name = "Preta",   id = "18637798529", code = "087SmokeBlack"},
})

Tab3:AddSection({"Corpos"})

--==================================================
-- FUNÃ‡ÃƒO BASE
--==================================================
local function ApplyBody(ids)

    Remotes.ChangeCharacterBody:InvokeServer({
        ids[1],
        ids[2],
        ids[3],
        ids[4],
        ids[5],
        ids[6]
    })

end




--==================================================
-- LISTA ORGANIZADA DE CORPOS
--==================================================
local Bodies = {

    ["PadrÃ£o"] = {

        { name = "Pequeno", ids = {376532000,376531012,376530220,0,0,71993263971199} },
        { name = "Musculoso", ids = {32336059,4416788553,4416785861,2517207746,2517204456,0} },
        { name = "Auto", ids = {84053421838284,116266039925754,115931454049085,96815508293389,121515016872144,76568859580605} },
        { name = "Mini", ids = {120973199097564,112849465115864,118345433416023,0,0,134082579} },
        { name = "1x1x1x", ids = {87108996849145,89124612345294,84431523780379,104281326156987,94645302037763,0} },
        { name = "Auto 2", ids = {15365110905,17754346380,17754346403,139607718,81153927159685,102360893332889} },
        { name = "Musculoso 2", ids = {18178775358,18178775182,18178775725,96815508293389,121515016872144,0} },
        { name = "Musculoso 3", ids = {32336117,16580491126,32336182,14752574049,14752574419,16580493236} },
        { name = "Mini REPO", ids = {117101023704825,125767940563838,137301494386930,87357384184710,133391239416999,111818794467824} },
        { name = "Mini GaranhÃ£o", ids = {124355047456535,120507500641962,82273782655463,113625313757230,109182039511426,0} },
        { name = "Stick", ids = {14731384498,14731377938,14731377894,14731377875,14731377941,14731382899} },
        { name = "Chunky-Bug", ids = {15527827600,15527827578,15527831669,15527836067,15527827184,15527827599} },
        { name = "Cursed-Spider", ids = {134555168634906,100269043793774,125607053187319,122504853343598,95907982259204,91289185840375} },
        { name = "Pessed Terror", ids = {122800511983371,132465361516275,125155800236527,83070163355072,102906187256945,78311422507297} },

    },

    ["Feminino"] = {

        { name = "Bunda CaÃ­da", ids = {75159926897589,76683091425509,14567282030,14567281990,14874108946,0} },
        { name = "Pequena Coxuda", ids = {84519338962111,104625992479605,89231963593670,101500262169377,130044271500910,0} },
        { name = "Pequena Cheinha", ids = {17759625390,17759625333,17759625415,17759626537,130044271500910,0} },
        { name = "Sla MoÃ§a", ids = {97119075795691,72699005696177,122027795919157,80393546275035,125637672664513,0} },

    }

}

--==================================================
-- CRIAR DROPDOWN AUTOMÃTICO
--==================================================
local function CreateBodyDropdown(categoryName, bodyTable)

    local options = {}

    for _, body in ipairs(bodyTable) do
        table.insert(options, body.name)
    end

    Tab3:AddDropdown({
        Name = categoryName,
        Options = options,
        Callback = function(selected)

            for _, body in ipairs(bodyTable) do
                if body.name == selected then
                    ApplyBody(body.ids)
                    break
                end
            end

        end
    })

end

--==================================================
-- GERAR TODOS AUTOMATICAMENTE
--==================================================
for categoryName, bodyTable in pairs(Bodies) do
    CreateBodyDropdown(categoryName, bodyTable)
end









Tab4:AddSection({"funÃ§Ã£o para usar em vocÃª"})

--=============================================================================
-- REMOVER BAN DAS CASAS
--=============================================================================
Tab4:AddButton({
    Name = "Remover Ban de Todas as Casas",
    Description = "Tenta remover o ban de todas as casas",
    Callback = function()

        local successCount = 0
        local failCount = 0

        for i = 1,37 do
            local bannedBlockName = "BannedBlock"..i
            local bannedBlock = workspace:FindFirstChild(bannedBlockName,true)

            if bannedBlock then
                local success = pcall(function()
                    bannedBlock:Destroy()
                end)

                if success then
                    successCount = successCount + 1
                else
                    failCount = failCount + 1
                end
            end
        end

        for _,house in pairs(workspace:GetDescendants()) do
            if house.Name:match("BannedBlock") then

                local success = pcall(function()
                    house:Destroy()
                end)

                if success then
                    successCount = successCount + 1
                else
                    failCount = failCount + 1
                end

            end
        end

        if successCount > 0 then
            game.StarterGui:SetCore("SendNotification",{
                Title = "Sucesso",
                Text = "Bans removidos de "..successCount.." casas!",
                Duration = 5
            })
        end

        if failCount > 0 then
            game.StarterGui:SetCore("SendNotification",{
                Title = "unban Aviso",
                Text = "Falha ao remover bans de "..failCount.." casas.",
                Duration = 5
            })
        end

        if successCount == 0 and failCount == 0 then
            game.StarterGui:SetCore("SendNotification",{
                Title = "Aviso",
                Text = "Nenhum ban encontrado para remover.",
                Duration = 5
            })
        end

    end
})




Tab4:AddSection({"para usar na sua casa"})

--=============================================================================
-- RGB CASA
--=============================================================================
Tab4:AddSection({"RGB Casa"})

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local rgbSpeed = 3
local rgbCasa = false

-- SLIDER VELOCIDADE
Tab4:AddSlider({
    Name = "Velocidade RGB",
    Min = 1,
    Max = 26,
    Default = 3,
    Callback = function(v)
        rgbSpeed = v
    end
})

-- FUNÃ‡ÃƒO COR RGB
local function getRainbowColor(speed)

    local h = (tick() * speed % 10) / 10
    return Color3.fromHSV(h,1,1)

end

-- FUNÃ‡ÃƒO REMOTE
local function fireServer(eventName,args)

    local RE = ReplicatedStorage:FindFirstChild("RE")

    if RE and RE:FindFirstChild(eventName) then
        pcall(function()
            RE[eventName]:FireServer(unpack(args))
        end)
    end

end

-- TOGGLE RGB CASA
Tab4:AddToggle({
    Name = "RGB Casa",
    Default = false,
    Callback = function(state)

        rgbCasa = state

        if state then

            task.spawn(function()

                while rgbCasa do

                    fireServer("1Player1sHous1e",{
                        "ColorPickHouse",
                        getRainbowColor(rgbSpeed)
                    })

                    task.wait(0.1)

                end

            end)

        end

    end
})

Tab4:AddSection({"loops na sua csa"})

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Remote = ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Player1sHous1e")

-- VariÃ¡veis de controle
local loopGarage = false
local loopCurtains = false
local loopBaby = false
local loopLock = false

--=============================================================================
-- GARAGE DOOR
--=============================================================================
Tab4:AddToggle({
    Name = "Abrir e Fechar Garagem loop",
    Default = false,
    Callback = function(state)
        loopGarage = state

        task.spawn(function()
            while loopGarage do
                Remote:FireServer("GarageDoor")
                task.wait(0.6)
            end
        end)
    end
})

--=============================================================================
-- CURTAINS
--=============================================================================
Tab4:AddToggle({
    Name = "abrir e fechar cortinas loop",
    Default = false,
    Callback = function(state)
        loopCurtains = state

        task.spawn(function()
            while loopCurtains do
                Remote:FireServer("Curtains")
                task.wait(0.6)
            end
        end)
    end
})

--=============================================================================
-- BABY OPTION
--=============================================================================
Tab4:AddToggle({
    Name = "Baby Option Loop",
    Default = false,
    Callback = function(state)
        loopBaby = state

        task.spawn(function()
            while loopBaby do
                Remote:FireServer("BabyOptionYes")
                task.wait(0.3)
            end
        end)
    end
})

--=============================================================================
-- LOCK DOORS
--=============================================================================
Tab4:AddToggle({
    Name = "Trancar Portas Loop",
    Default = false,
    Callback = function(state)
        loopLock = state

        task.spawn(function()
            while loopLock do
                Remote:FireServer("LockDoors")
                task.wait(0.6)
            end
        end)
    end
})



pcall(function()
    Tab4:AddSection({ Name = "Teleporte Para Casas" })
end)

-- Lista de casas para teletransporte
local casas = {
    ["Casa 1"] = Vector3.new(260.29, 4.37, 209.32),
    ["Casa 2"] = Vector3.new(234.49, 4.37, 228.00),
    ["Casa 3"] = Vector3.new(262.79, 21.37, 210.84),
    ["Casa 4"] = Vector3.new(229.60, 21.37, 225.40),
    ["Casa 5"] = Vector3.new(173.44, 21.37, 228.11),
    ["Casa 6"] = Vector3.new(-43, 21, -137),
    ["Casa 7"] = Vector3.new(-40, 36, -137),
    ["Casa 11"] = Vector3.new(-21, 40, 436),
    ["Casa 12"] = Vector3.new(155, 37, 433),
    ["Casa 13"] = Vector3.new(255, 35, 431),
    ["Casa 14"] = Vector3.new(254, 38, 394),
    ["Casa 15"] = Vector3.new(148, 39, 387),
    ["Casa 16"] = Vector3.new(-17, 42, 395),
    ["Casa 17"] = Vector3.new(-189, 37, -247),
    ["Casa 18"] = Vector3.new(-354, 37, -244),
    ["Casa 19"] = Vector3.new(-456, 36, -245),
    ["Casa 20"] = Vector3.new(-453, 38, -295),
    ["Casa 21"] = Vector3.new(-356, 38, -294),
    ["Casa 22"] = Vector3.new(-187, 37, -295),
    ["Casa 23"] = Vector3.new(-410, 68, -447),
    ["Casa 24"] = Vector3.new(-348, 69, -496),
    ["Casa 28"] = Vector3.new(-103, 12, 1087),
    ["Casa 29"] = Vector3.new(-730, 6, 808),
    ["Casa 30"] = Vector3.new(-245, 7, 822),
    ["Casa 31"] = Vector3.new(639, 76, -361),
    ["Casa 32"] = Vector3.new(-908, 6, -361),
    ["Casa 33"] = Vector3.new(-111, 70, -417),
    ["Casa 34"] = Vector3.new(230, 38, 569),
    ["Casa 35"] = Vector3.new(-30, 13, 2209)
}

-- Criar lista de nomes de casas ordenada
local casasNomes = {}
for nome, _ in pairs(casas) do
    table.insert(casasNomes, nome)
end

table.sort(casasNomes, function(a, b)
    local numA = tonumber(a:match("%d+")) or 0
    local numB = tonumber(b:match("%d+")) or 0
    return numA < numB
end)

-- Dropdown para teletransporte
pcall(function()
    Tab4:AddDropdown({
        Name = "Selecionar Casa",
        Options = casasNomes,
        Callback = function(casaSelecionada)
            local player = game.Players.LocalPlayer
            if player and player.Character then
                player.Character.HumanoidRootPart.CFrame = CFrame.new(casas[casaSelecionada])
            end
        end
    })
end)

-- Label apÃƒÂ³s dropdown
pcall(function()
    Tab4:AddLabel("Teleporte para a Casa que Quiser")
end)


--=============================================================================
-- PARÃGRAFO FINAL
--=============================================================================
Tab4:AddParagraph({
    Title = "por em quanto so isso :p",
    Content = ""
})





Tab5:AddSection({"Speed Car/Boat & Turbo"})

--==================================================
-- VALORES
--==================================================

local speedValue = 200
local turboValue = 11.3

--==================================================
-- FUNÃ‡Ã•ES
--==================================================

-- Carro
local function applyCarSpeed(vehicle)

    local seat = vehicle:FindFirstChild("Seats") and vehicle.Seats:FindFirstChild("VehicleSeat")
    if not seat then return end

    pcall(function()
        seat.MaxSpeed = speedValue
    end)

    for _, v in pairs(seat:GetChildren()) do
        if v:IsA("NumberValue") then
            v.Value = speedValue
        end
    end

end


local function applyCarTurbo(vehicle)

    local seat = vehicle:FindFirstChild("Seats") and vehicle.Seats:FindFirstChild("VehicleSeat")
    if not seat then return end

    pcall(function()
        seat.Turbo = turboValue
    end)

    for _, v in pairs(seat:GetChildren()) do
        if v:IsA("NumberValue") and v.Name:lower():find("turbo") then
            v.Value = turboValue
        end
    end

end


-- Barco
local function applyBoatSpeed(vehicle)

    local body = vehicle:FindFirstChild("Body")
    if not body then return end

    local seat = body:FindFirstChild("VehicleSeat")

    if seat then
        pcall(function()
            seat.MaxSpeed = speedValue
        end)
    end

    -- forÃ§a valores internos
    for _, obj in pairs(body:GetDescendants()) do
        if obj:IsA("NumberValue") then
            obj.Value = speedValue
        end
    end

    -- fallback fÃ­sico
    if vehicle.PrimaryPart then
        vehicle.PrimaryPart.AssemblyLinearVelocity =
            vehicle.PrimaryPart.CFrame.LookVector * speedValue
    end

end


--==================================================
-- APPLY GERAL
--==================================================

local function applySpeedAll()

    local folder = workspace:FindFirstChild("Vehicles")
    if not folder then return end

    local count = 0

    for _, v in pairs(folder:GetChildren()) do
        if v:FindFirstChild("Seats") then
            applyCarSpeed(v)
            count += 1
        elseif v:FindFirstChild("Body") then
            applyBoatSpeed(v)
            count += 1
        end
    end

    print("Speed aplicado em:", count, "veÃ­culos")

end


local function applyTurboAll()

    local folder = workspace:FindFirstChild("Vehicles")
    if not folder then return end

    local count = 0

    for _, v in pairs(folder:GetChildren()) do
        if v:FindFirstChild("Seats") then
            applyCarTurbo(v)
            count += 1
        end
    end

    print("Turbo aplicado em:", count, "carros")

end


--==================================================
-- UI
--==================================================

Tab5:AddTextBox({
    Name = "Speed",
    PlaceholderText = "200",
    Callback = function(v)
        local n = tonumber(v)
        if n then speedValue = n end
    end
})

Tab5:AddTextBox({
    Name = "Turbo",
    PlaceholderText = "11.3",
    Callback = function(v)
        local n = tonumber(v)
        if n then turboValue = n end
    end
})

Tab5:AddButton({
    Name = "Aplicar Speed",
    Callback = function()
        applySpeedAll()
    end
})

Tab5:AddButton({
    Name = "Aplicar Turbo",
    Callback = function()
        applyTurboAll()
    end
})



Tab5:AddSection({"loops"})

--=============================================================================
-- REMOTES DE VEÃCULO (LOOPS 0.5s)
--=============================================================================
local Remotes = game:GetService("ReplicatedStorage"):WaitForChild("Remotes")
local CarRemote = game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Player1sCa1r")

-- VariÃ¡veis de controle
local loopBuzina = false
local loopFarol = false
local loopLights = false
local loopDescapotar = false
local loopMusica = false

--=============================================================================
-- LOOP BUZINA
--=============================================================================
Tab5:AddToggle({
    Name = "Loop Buzina",
    Default = false,
    Callback = function(state)
        loopBuzina = state

        task.spawn(function()
            while loopBuzina do
                Remotes.PlayHorn:InvokeServer()
                Remotes.StopHorn:InvokeServer()
                task.wait(0.5)
            end
        end)
    end
})

--=============================================================================
-- LOOP FAROL
--=============================================================================
Tab5:AddToggle({
    Name = "Loop Farol",
    Default = false,
    Callback = function(state)
        loopFarol = state

        task.spawn(function()
            while loopFarol do
                Remotes.ToggleHeadLights:InvokeServer()
                task.wait(0.5)
            end
        end)
    end
})

--=============================================================================
-- LOOP LIGHTS (ALERTA)
--=============================================================================
Tab5:AddToggle({
    Name = "Loop Lights",
    Default = false,
    Callback = function(state)
        loopLights = state

        task.spawn(function()
            while loopLights do
                Remotes.ToggleHazardLights:InvokeServer()
                task.wait(0.5)
            end
        end)
    end
})

--=============================================================================
-- LOOP DESCAPOTAR
--=============================================================================
Tab5:AddToggle({
    Name = "Loop Descapotar",
    Default = false,
    Callback = function(state)
        loopDescapotar = state

        task.spawn(function()
            while loopDescapotar do
                Remotes.RepositionVehicle:InvokeServer()
                task.wait(0.5)
            end
        end)
    end
})

--=============================================================================
-- LOOP MÃšSICA CHATA
--=============================================================================
Tab5:AddToggle({
    Name = "Loop MÃºsica Chata",
    Default = false,
    Callback = function(state)
        loopMusica = state

        task.spawn(function()
            while loopMusica do
                local args = {
                    [1] = "PickingVehicleMusicText",
                    [2] = "138246178191037",
                    [4] = true
                }

                CarRemote:FireServer(unpack(args))
                task.wait(0.5)
            end
        end)
    end
})



local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

local TeleportCarro = {}

-- LISTA DE CARROS
function TeleportCarro:AtualizarListaCarros()
    local lista = {}
    local pasta = Workspace:FindFirstChild("Vehicles")

    if pasta then
        for _, car in ipairs(pasta:GetChildren()) do
            if car.Name:match("Car$") then
                table.insert(lista, car.Name)
            end
        end
    end

    return lista
end

-- PEGAR CARRO
function TeleportCarro:GetVehicle(nome)
    local pasta = Workspace:FindFirstChild("Vehicles")
    if not pasta then return end
    return pasta:FindFirstChild(nome)
end

-- SENTAR NO CARRO
function TeleportCarro:Sentar(seat)
    local char = LocalPlayer.Character
    if not char then return false end

    local hum = char:FindFirstChild("Humanoid")
    local root = char:FindFirstChild("HumanoidRootPart")

    if not hum or not root then return false end

    hum.Sit = false
    task.wait(0.1)

    root.CFrame = seat.CFrame + Vector3.new(0,5,0)
    task.wait(0.1)

    seat:Sit(hum)
    task.wait(0.3)

    return hum.SeatPart == seat
end

-- SAIR DO CARRO
function TeleportCarro:Sair(pos)
    local char = LocalPlayer.Character
    if not char then return end

    local hum = char:FindFirstChild("Humanoid")
    if hum and hum.SeatPart then
        hum.Sit = false
    end

    task.wait(0.1)

    if pos then
        char:PivotTo(CFrame.new(pos))
    end
end

-- TELEPORTAR CARRO
function TeleportCarro:Trazer(vehicle, pos)
    if not vehicle then return end

    if not vehicle.PrimaryPart then
        local p = vehicle:FindFirstChild("Body", true) or vehicle:FindFirstChild("Chassis", true)
        if p then vehicle.PrimaryPart = p else return end
    end

    vehicle:SetPrimaryPartCFrame(CFrame.new(pos + Vector3.new(5,0,5)))
end

-- ================= UI =================

local SelectedVehicle = nil

local Dropdown = Tab5:AddDropdown({
    Name = "Selecionar Carro",
    Options = TeleportCarro:AtualizarListaCarros(),
    Callback = function(v)
        SelectedVehicle = v
    end
})


-- 🔥 BOTÃO DE ATUALIZAR LISTA (MANUAL)
Tab5:AddButton({
    Name = "Atualizar Lista de Carros",
    Callback = function()
        Dropdown:Set(TeleportCarro:AtualizarListaCarros())
    end
})




-- 🔥 VISUALIZAR CARRO
Tab5:AddToggle({
    Name = "Ver Camera do Carro",
    Default = false,
    Callback = function(state)
        if state then
            if not SelectedVehicle then return end

            local vehicle = TeleportCarro:GetVehicle(SelectedVehicle)
            if not vehicle then return end

            local seat = vehicle:FindFirstChildWhichIsA("VehicleSeat", true)
            if not seat then return end

            TeleportCarro.OldSubject = Camera.CameraSubject
            TeleportCarro.OldType = Camera.CameraType

            Camera.CameraSubject = seat
            Camera.CameraType = Enum.CameraType.Follow
        else
            if TeleportCarro.OldSubject then
                Camera.CameraSubject = TeleportCarro.OldSubject
                Camera.CameraType = TeleportCarro.OldType or Enum.CameraType.Custom
            end
        end
    end
})

-- 🔥 TRAZER CARRO
Tab5:AddButton({
    Name = "Trazer o carro selecionado",
    Callback = function()
        if not SelectedVehicle then return end

        local vehicle = TeleportCarro:GetVehicle(SelectedVehicle)
        if not vehicle then return end

        local seat = vehicle:FindFirstChildWhichIsA("VehicleSeat", true)
        if not seat or seat.Occupant then return end

        local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if not root then return end

        local pos = root.Position

        local success = TeleportCarro:Sentar(seat)
        if success then
            TeleportCarro:Trazer(vehicle, pos)
            TeleportCarro:Sair(pos)
        end
    end
})

-- 🔥 TRAZER TODOS OS CARROS
Tab5:AddButton({
    Name = "Trazer Todos os Carros",
    Callback = function()
        local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if not root then return end

        local pos = root.Position
        local pasta = Workspace:FindFirstChild("Vehicles")
        if not pasta then return end

        for _, car in ipairs(pasta:GetChildren()) do
            if car.Name:match("Car$") then
                local seat = car:FindFirstChildWhichIsA("VehicleSeat", true)

                if seat and not seat.Occupant then
                    local success = TeleportCarro:Sentar(seat)
                    if success then
                        TeleportCarro:Trazer(car, pos)
                        TeleportCarro:Sair(pos)
                        task.wait(1)
                    end
                end
            end
        end
    end
})




print("Speed Car / Speed Boat ForÃ§ado | Turbo Car OK")









--////////////////////////////////////////////////////
--// PLAYER LIST
local selectedPlayer

local function getPlayers()
    local t = {}

    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= plr then
            table.insert(t, p.Name)
        end
    end

    return t
end

local dropdown = Tab6:AddDropdown({
    Name = "Escolher Player",
    Options = getPlayers(),
    Callback = function(v)
        selectedPlayer = v
    end
})

Tab6:AddButton({
    Name = "Atualizar Lista",
    Callback = function()
        dropdown:Set(getPlayers())
    end
})

--====================================================
-- SPECTATE
--====================================================
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

local spectating = false
local spectateConn
local charConn
local currentTarget

-- RESET CAMERA
local function resetCamera()

    if spectateConn then
        spectateConn:Disconnect()
        spectateConn = nil
    end

    if charConn then
        charConn:Disconnect()
        charConn = nil
    end

    currentTarget = nil

    local char = LocalPlayer.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")

    if hum then
        Camera.CameraSubject = hum
        Camera.CameraType = Enum.CameraType.Custom
    end

end

-- SPECTAR PLAYER
local function spectatePlayer(player)

    if not spectating or not player then return end

    resetCamera()

    spectating = true
    currentTarget = player

    spectateConn = RunService.RenderStepped:Connect(function()

        if not spectating then return end
        if not currentTarget or not currentTarget.Character then return end

        local hum = currentTarget.Character:FindFirstChildOfClass("Humanoid")

        if hum then
            Camera.CameraSubject = hum
            Camera.CameraType = Enum.CameraType.Custom
        end

    end)

    charConn = player.CharacterAdded:Connect(function()
        task.wait(0.1)

        if spectating and currentTarget == player then
            spectatePlayer(player)
        end
    end)

end

-- PLAYER SAINDO
Players.PlayerRemoving:Connect(function(player)

    if spectating and currentTarget == player then
        resetCamera()
        spectating = false
    end

end)





--====================================================
-- TOGGLE SPECTATE
--====================================================
Tab6:AddToggle({
    Name = "Spectar Jogador",
    Default = false,
    Callback = function(state)

        spectating = state

        if not state then
            resetCamera()
            return
        end

        local target = Players:FindFirstChild(selectedPlayer)

        if target then
            spectatePlayer(target)
        else
            resetCamera()
        end

    end
})

--====================================================
-- TROCAR PLAYER SEM QUEBRAR
--====================================================
task.spawn(function()

    local lastSelected

    while true do
        task.wait(0.15)

        if not spectating then
            lastSelected = nil
            continue
        end

        if selectedPlayer ~= lastSelected then

            lastSelected = selectedPlayer

            local newTarget = Players:FindFirstChild(selectedPlayer)

            if newTarget then
                spectatePlayer(newTarget)
            else
                resetCamera()
            end

        end
    end

end)




--====================================================
-- TELEPORTAR PARA PLAYER
--====================================================
Tab6:AddButton({
    Name = "Teleportar no Jogador",
    Callback = function()

        if not selectedPlayer then return end

        local target = Players:FindFirstChild(selectedPlayer)
        if not target or not target.Character then return end

        local myChar = plr.Character
        local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
        local targetHRP = target.Character:FindFirstChild("HumanoidRootPart")

        if myHRP and targetHRP then
            myHRP.CFrame = targetHRP.CFrame * CFrame.new(0,0,-3)
        end

    end
})



Tab6:AddSection({"metodos"})

local selectedKillMethod = "Sofa"

--------------------------------------------------
-- DROPDOWN
--------------------------------------------------
Tab6:AddDropdown({
    Name = "Escolher Kill",
    Options = {"Sofa", "Bus"},
    Default = "Sofa",
    Callback = function(v)
        selectedKillMethod = v
    end
})

--------------------------------------------------
-- VARIÁVEIS
--------------------------------------------------
local running = false
local seated = false
local sitConn
local killRunning = false

--------------------------------------------------
-- RESET (QUANDO MORRE OU RESPAWNA)
--------------------------------------------------
plr.CharacterAdded:Connect(function()
    running = false
    seated = false
    killRunning = false

    if sitConn then
        sitConn:Disconnect()
        sitConn = nil
    end
end)

--------------------------------------------------
-- BOTÃO KILL
--------------------------------------------------
Tab6:AddButton({
    Name = "kill",
    Callback = function()

        if not selectedPlayer then
            warn("Nenhum jogador selecionado!")
            return
        end

        --------------------------------------------------
        -- SOFA
        --------------------------------------------------
        if selectedKillMethod == "Sofa" then

            if running then return end

            local character = plr.Character
            if not character then return end

            local humanoid = character:FindFirstChildOfClass("Humanoid")
            local hrp = character:FindFirstChild("HumanoidRootPart")
            if not humanoid or not hrp then return end

            local target = Players:FindFirstChild(selectedPlayer)
            if not target or not target.Character then return end

            local targetHumanoid = target.Character:FindFirstChild("Humanoid")
            local targetHRP = target.Character:FindFirstChild("HumanoidRootPart")
            if not targetHumanoid or not targetHRP then return end

            running = true
            seated = false

            local hasCouch = false

            if character:FindFirstChild("Couch") then
                hasCouch = true
            end

            for _,tool in ipairs(plr.Backpack:GetChildren()) do
                if tool.Name == "Couch" then
                    hasCouch = true
                end
            end

            if not hasCouch then
                ReplicatedStorage.RE["1Too1l"]:InvokeServer("PickingTools","Couch")
                task.wait(0.3)
            end

            local couch = plr.Backpack:FindFirstChild("Couch")
            if couch then
                humanoid:EquipTool(couch)
            end

            task.wait(0.2)

            hrp.CFrame = targetHRP.CFrame * CFrame.new(0,0,-2)

            if sitConn then
                sitConn:Disconnect()
            end

            sitConn = targetHumanoid.Seated:Connect(function(active)
                if active and running then -- só processa se ainda estiver rodando
                    seated = true
                    running = false

                    sitConn:Disconnect()

                    hrp.CFrame = CFrame.new(-54.24,-298.21,-184.12)

                    task.wait(0.3)

                    humanoid:UnequipTools()

                    task.wait(0.7)

                    hrp.CFrame = CFrame.new(-27.59,4.27,-16.84)
                end
            end)

            -- Movimento em volta do alvo
            task.spawn(function()
                local tickSide = 1

                while running and not seated do
                    if not targetHRP or not targetHRP.Parent then break end -- segurança
                    local base = targetHRP.Position

                    local offsets = {
                        Vector3.new(3 * tickSide,0,0),
                        Vector3.new(-3 * tickSide,0,0),
                        Vector3.new(0,0,3 * tickSide),
                        Vector3.new(0,0,-3 * tickSide),
                        Vector3.new(0,3,0),
                        Vector3.new(0,-3,0),
                        Vector3.new(1.5,-3.2,1.5),
                        Vector3.new(-1.5,-3.2,-1.5)
                    }

                    for _,off in ipairs(offsets) do
                        if not running or seated then break end
                        hrp.CFrame = CFrame.new(base + off, base)
                        task.wait(0.035)
                    end

                    tickSide = -tickSide
                end
            end)

            -- Executar CycleNextAnimation MUITO RÁPIDO até sentar ou parar
            task.spawn(function()
                local paintRoller = character:FindFirstChild("PaintRoller")
                while running and not seated do
                    if paintRoller and paintRoller:FindFirstChild("CycleNextAnimation") then
                        paintRoller.CycleNextAnimation:FireServer()
                    end
                    task.wait(0.01) -- muito rápido
                end
            end)

        --------------------------------------------------
        -- BUS
        --------------------------------------------------
        elseif selectedKillMethod == "Bus" then

            if killRunning then return end
            killRunning = true

            local Player = game.Players.LocalPlayer
            local Character = Player.Character or Player.CharacterAdded:Wait()
            local Humanoid = Character:FindFirstChildOfClass("Humanoid")
            local RootPart = Character:FindFirstChild("HumanoidRootPart")
            local Vehicles = workspace:WaitForChild("Vehicles")
            local OldPos = RootPart and RootPart.CFrame

            if not Humanoid or not RootPart then killRunning = false return end

            -- Spawn inicial
            RootPart.CFrame = CFrame.new(1118.81, 75.998, -1138.61)
            task.wait(0.5)

            local PCar = Vehicles:FindFirstChild(Player.Name.."Car")

            if not PCar then
                game.ReplicatedStorage.RE:FindFirstChild("1Ca1r"):FireServer("PickingCar", "SchoolBus")
                task.wait(1.5)
                PCar = Vehicles:FindFirstChild(Player.Name.."Car")
            end

            if not PCar then killRunning = false return end

            -- Vai até o ônibus
            RootPart.CFrame = CFrame.new(1108.079956, 78.852867, -1146.426147)
            task.wait(0.4)

            local Seat = PCar:FindFirstChild("Body") and PCar.Body:FindFirstChild("VehicleSeat")

            if Seat then
                for i = 1, 10 do
                    if not killRunning then break end
                    Seat:Sit(Humanoid)
                    task.wait(0.3)
                    if Humanoid.Sit then break end
                end
            end

            if PCar and not PCar.PrimaryPart then
                local Body = PCar:FindFirstChild("Body")
                if Body and Body:FindFirstChild("VehicleSeat") then
                    PCar.PrimaryPart = Body.VehicleSeat
                end
            end

            if not Humanoid.Sit then
                killRunning = false
                return
            end

            local TargetPlayer = Players:FindFirstChild(selectedPlayer)
            if not TargetPlayer then killRunning = false return end

            local TargetC = TargetPlayer.Character
            local TargetH = TargetC and TargetC:FindFirstChildOfClass("Humanoid")
            local TargetRP = TargetC and TargetC:FindFirstChild("HumanoidRootPart")

            if not (TargetC and TargetH and TargetRP) then killRunning = false return end

            local targetSeated = false
            local targetSeatConn
            targetSeatConn = TargetH.Seated:Connect(function(active)
                if active and killRunning then
                    targetSeated = true
                end
            end)

            -- Loop de movimento aleatório
            task.spawn(function()
                while killRunning and Humanoid.Sit and not targetSeated and TargetH.Health > 0 and TargetRP and PCar and PCar.PrimaryPart do
                    local randomX = math.random(-180, 180)
                    local randomY = math.random(-180, 180)
                    local randomZ = math.random(-180, 180)

                    local offset = TargetH.MoveDirection * (TargetRP.Velocity.Magnitude / 1.05)

                    local relativeOffsets = {
                        CFrame.new(0, 1, 0),
                        CFrame.new(0, -2.25, 5),
                        CFrame.new(0, 2.25, 0.25),
                        CFrame.new(-2.25, -1.5, 2.25),
                        CFrame.new(0, 1.5, 0),
                        CFrame.new(0, -1.5, 0)
                    }

                    local chosenOffset = relativeOffsets[math.random(1, #relativeOffsets)]
                    local newCF = TargetRP.CFrame * chosenOffset * CFrame.new(offset) * CFrame.Angles(math.rad(randomX), math.rad(randomY), math.rad(randomZ))

                    PCar:PivotTo(newCF)
                    task.wait(0.08)
                end
            end)

            -- Aguarda alvo sentar ou killRunning ser falso
            while killRunning and not targetSeated and TargetH.Health > 0 do
                task.wait(0.2)
            end

            -- Se killRunning foi desligado manualmente (botão Parar), cancela a descida
            if not killRunning then
                if targetSeatConn then targetSeatConn:Disconnect() end
                Humanoid.Sit = false
                if OldPos then RootPart.CFrame = OldPos end
                game:GetService("ReplicatedStorage").RE:FindFirstChild("1Ca1r"):FireServer("DeleteAllVehicles")
                return
            end

            -- Se chegou aqui, o alvo sentou e killRunning continua true
            if PCar and PCar.PrimaryPart then
                PCar:PivotTo(CFrame.new(0, -300, 0))

                local reached = false
                local start = tick()
                while killRunning and not reached and (tick() - start) < 10 do
                    if PCar and PCar.PrimaryPart and PCar.PrimaryPart.Position.Y <= -400 then
                        reached = true
                    end
                    task.wait(0.1)
                end

                if killRunning and reached then
                    if Humanoid and Humanoid.Health > 0 then
                        Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                    end
                    task.wait(0.2)
                end

                Humanoid.Sit = false
                task.wait(0.1)

                if OldPos then
                    RootPart.CFrame = OldPos
                end

                game:GetService("ReplicatedStorage").RE:FindFirstChild("1Ca1r"):FireServer("DeleteAllVehicles")
            else
                Humanoid.Sit = false
                if OldPos then RootPart.CFrame = OldPos end
                game:GetService("ReplicatedStorage").RE:FindFirstChild("1Ca1r"):FireServer("DeleteAllVehicles")
            end

            if targetSeatConn then targetSeatConn:Disconnect() end
            killRunning = false
        end

    end
})

--------------------------------------------------
-- BOTÃO PARAR (INTERROMPE TUDO)
--------------------------------------------------
Tab6:AddButton({
    Name = "Parar",
    Callback = function()
        -- Sofá
        running = false
        if sitConn then
            sitConn:Disconnect()
            sitConn = nil
        end

        -- Ônibus
        killRunning = false

        -- Se estiver no ônibus, tenta sair e deletar veículo
        local Player = game.Players.LocalPlayer
        local Character = Player.Character
        if Character then
            local Humanoid = Character:FindFirstChildOfClass("Humanoid")
            local RootPart = Character:FindFirstChild("HumanoidRootPart")
            if Humanoid and Humanoid.Sit then
                Humanoid.Sit = false
            end
            -- Tenta deletar veículo (mesmo que não esteja mais no loop)
            pcall(function()
                game:GetService("ReplicatedStorage").RE:FindFirstChild("1Ca1r"):FireServer("DeleteAllVehicles")
            end)
            -- Opcional: teleportar para uma posição segura? Não incluso.
        end

        warn("Kill interrompido.")
    end
})



Tab6:AddSection({"pirate"})



local runningGlobal = false
local stopConnection

Tab6:AddButton({
    Name = "bring boat pirate",
    Callback = function()
        if runningGlobal then return end -- Evita mÃºltiplos spawns
        if not selectedPlayer then return end

        runningGlobal = true

        local Players = game:GetService("Players")
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local RunService = game:GetService("RunService")
        local Workspace = game:GetService("Workspace")

        local plr = Players.LocalPlayer
        local character = plr.Character
        if not character then 
            runningGlobal = false
            return 
        end

        local humanoid = character:FindFirstChildOfClass("Humanoid")
        local hrp = character:FindFirstChild("HumanoidRootPart")

        if not humanoid or not hrp then 
            runningGlobal = false
            return 
        end

        local target = Players:FindFirstChild(selectedPlayer)
        if not target or not target.Character then 
            runningGlobal = false
            return 
        end

        local targetHumanoid = target.Character:FindFirstChild("Humanoid")
        local targetHRP = target.Character:FindFirstChild("HumanoidRootPart")

        if not targetHumanoid or not targetHRP then 
            runningGlobal = false
            return 
        end

        local Vehicles = workspace:WaitForChild("Vehicles")

        --==============================
        -- SPAWN BARCO (DO TORNADO)
        --==============================
        hrp.CFrame = CFrame.new(1754, -2, 58)
        task.wait(0.5)

        ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer("PickingBoat","PirateFree")
        task.wait(1)

        local PCar = Vehicles:FindFirstChild(plr.Name .. "Car")
        if not PCar then 
            runningGlobal = false
            return 
        end

        local Seat = PCar:FindFirstChild("Body") and PCar.Body:FindFirstChild("VehicleSeat")
        if not Seat then 
            runningGlobal = false
            return 
        end

        --==============================
        -- SENTAR (DO TORNADO)
        --==============================
        repeat
            hrp.CFrame = Seat.CFrame * CFrame.new(0,1,0)
            task.wait()
        until humanoid.SeatPart == Seat or not runningGlobal

        if not runningGlobal then return end

        humanoid.Sit = true
        task.wait(0.2)

        -- VerificaÃ§Ã£o constante se ainda estÃ¡ sentado
        local seatedCheckConn
        seatedCheckConn = RunService.Heartbeat:Connect(function()
            if not runningGlobal then 
                seatedCheckConn:Disconnect()
                return 
            end
            
            if humanoid.SeatPart ~= Seat then
                -- Parar tudo se nÃ£o estiver mais sentado
                runningGlobal = false
                seatedCheckConn:Disconnect()
                humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end)

        --==============================
        -- FULL CAOS MOVEMENT (MAIS RÃPIDO)
        --==============================
        task.spawn(function()
            local tickSide = 1
            local rot = 0

            while runningGlobal do
                if not target.Character or not targetHRP or humanoid.SeatPart ~= Seat then 
                    break 
                end

                local base = targetHRP.Position
                rot += math.rad(60) -- Mais rÃ¡pido na rotaÃ§Ã£o

                local offsets = {
                    Vector3.new(1.5,0,0),
                    Vector3.new(-1.5,0,0),
                    Vector3.new(0,0,1.5),
                    Vector3.new(0,0,-1.5),
                    Vector3.new(0,2,0),
                    Vector3.new(0,-2,0),
                    Vector3.new(2,1,2),
                    Vector3.new(-2,1,-2),
                    Vector3.new(2,-2,2),
                    Vector3.new(-2,-2,-2),
                    Vector3.new(0,3,1),
                    Vector3.new(0,-3,-1),
                    Vector3.new(1,2,-2),
                    Vector3.new(-1,-2,2),
                    Vector3.new(3,0,1),
                    Vector3.new(-3,0,-1),
                    Vector3.new(1.2,-3.2,1.2),
                    Vector3.new(-1.2,-3.2,-1.2)
                }

                for _,off in ipairs(offsets) do
                    if not runningGlobal then break end
                    hrp.CFrame = CFrame.new(base + off, base) * CFrame.Angles(rot, rot, rot)
                    task.wait(0.005) -- Muito mais rÃ¡pido (era 0.01)
                end
            end
        end)
    end
})

-- BotÃ£o de PARAR
Tab6:AddButton({
    Name = "Parar Boat Pirate",
    Callback = function()
        runningGlobal = false
        
        local plr = game.Players.LocalPlayer
        local character = plr.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end
        
        if stopConnection then
            stopConnection:Disconnect()
        end
    end
})



Tab6:AddSection({"flings"})





--////////////////////////////////////////////////////
--// FLING CANOE COMPLETO + TP FIX (SEM RECUO)
Tab6:AddButton({
    Name = "Fling Canoe ",
    Description = "Fling",
    Callback = function()

        local nome = selectedPlayer
        if not nome then return end

        local AlvoSelecionado = game.Players:FindFirstChild(nome)
        if not AlvoSelecionado then return end

        local player = game.Players.LocalPlayer
        local char = player.Character or player.CharacterAdded:Wait()
        local humanoid = char:WaitForChild("Humanoid")
        local root = char:WaitForChild("HumanoidRootPart")

        -- sair de qualquer assento
        if humanoid.Sit then
            humanoid.Sit = false
            task.wait(0.5)
        end

        -- spawn da canoa
        root.CFrame = workspace.WorkspaceCom["001_CanoeCloneButton"].Button.CFrame
        task.wait(0.4)
        fireclickdetector(workspace.WorkspaceCom["001_CanoeCloneButton"].Button.ClickDetector, 0)
        task.wait(0.4)

        local canoe = workspace.WorkspaceCom["001_CanoeStorage"].Canoe
        local seat = canoe:FindFirstChild("VehicleSeat")

        if not canoe.PrimaryPart then
            canoe.PrimaryPart = seat
        end

        -- sentar na canoa
        local tentativas = 0
        repeat
            char:MoveTo(seat.Position + Vector3.new(0, 3, 0))
            task.wait(0.05)
            seat:Sit(humanoid)
            tentativas += 1
        until humanoid.Sit or tentativas > 100

        if not humanoid.Sit then
            warn("NÃ£o sentou na canoa")
            return
        end

        -- alvo
        local alvoChar = AlvoSelecionado.Character or AlvoSelecionado.CharacterAdded:Wait()
        local alvoRoot = alvoChar:WaitForChild("HumanoidRootPart")
        local alvoHum = alvoChar:WaitForChild("Humanoid")

        local force = Instance.new("BodyForce", canoe.PrimaryPart)
        local angular = Instance.new("BodyAngularVelocity", canoe.PrimaryPart)

        angular.MaxTorque = Vector3.new(1e9, 1e9, 1e9)
        angular.AngularVelocity = Vector3.new(1000, 3000, 1000)
        angular.P = 1e9

        local ativouFinal = false
        local start = tick()

        -- LOOP DO FLING
        while tick() - start < 10 do
            if not humanoid.Sit then break end
            if not alvoChar or not alvoHum or alvoHum.Health <= 0 then break end

            local look = alvoRoot.CFrame.LookVector
            local right = alvoRoot.CFrame.RightVector

            local pos = alvoRoot.Position
                + look * math.random(-5, 8)
                + right * math.random(-3, 6)
                + Vector3.new(0, math.random(0, 4), 0)

            canoe:SetPrimaryPartCFrame(CFrame.new(pos, alvoRoot.Position))

            local dir = (alvoRoot.Position - canoe.PrimaryPart.Position).Unit
            force.Force = dir * 1e6 + Vector3.new(0, workspace.Gravity * canoe.PrimaryPart:GetMass(), 0)

            -- detectar se alvo sentou
            local sentado = alvoHum.SeatPart ~= nil 
                or alvoHum:GetState() == Enum.HumanoidStateType.Seated

            if sentado and not ativouFinal then
                ativouFinal = true
                break
            end

            task.wait(0.03)
        end

        force:Destroy()
        angular:Destroy()

        -- SEQUÃŠNCIA FINAL COM FIX
        if ativouFinal then
            -- TP pra cima
            root.CFrame = CFrame.new(0, 100000, 0)

            -- pulo
            humanoid:ChangeState(Enum.HumanoidStateType.Jumping)

            task.wait(0.2)

            -- posiÃ§Ã£o final
            local finalPos = CFrame.new(-7.20, 3.67, -37.54)

            -- ðŸ”’ SEGURAR NO LUGAR (anti-bug)
            local tempo = tick()
            while tick() - tempo < 1 do
                root.CFrame = finalPos
                task.wait()
            end
        end
    end
})




local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer
local cam = workspace.CurrentCamera

local flingCrochActive = false

---------------------------------------------------
-- FUNÃ‡ÃƒO QUE FAZ O FLING
---------------------------------------------------

local function doFlingSofa()

    if not selectedPlayer then return end

    local target = Players:FindFirstChild(selectedPlayer)
    if not target or not target.Character then return end

    local char = LocalPlayer.Character
    if not char then return end

    local root = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")

    local tRoot = target.Character:FindFirstChild("HumanoidRootPart")
    local tHum = target.Character:FindFirstChildOfClass("Humanoid")

    if not (root and hum and tRoot and tHum) then return end

    -- CLEAR TOOLS
    local clearRemote = ReplicatedStorage.RE:FindFirstChild("1Clea1rTool1s")
    if clearRemote then
        clearRemote:FireServer("ClearAllTools")
    end

    task.wait(0.3)

    -- PEGAR SOFÃ
    local toolRemote = ReplicatedStorage.RE:FindFirstChild("1Too1l")
    if toolRemote then
        toolRemote:InvokeServer("PickingTools","Couch")
    end

    local original = root.CFrame

    local tool = LocalPlayer.Backpack:FindFirstChildOfClass("Tool")
    if tool then
        tool.Parent = char
    end

    workspace.FallenPartsDestroyHeight = -math.huge

    local bv = Instance.new("BodyVelocity")
    bv.Name = "FlingForce"
    bv.Velocity = Vector3.new(9e8,9e8,9e8)
    bv.MaxForce = Vector3.new(math.huge,math.huge,math.huge)
    bv.Parent = root

    hum:SetStateEnabled(Enum.HumanoidStateType.Seated,false)
    hum.PlatformStand = false

    cam.CameraSubject = tRoot

    local angle = 0
    local t = tick()

    while tick() - t < 3
    and target
    and target.Character
    and target.Character:FindFirstChildOfClass("Humanoid") do

        tRoot = target.Character:FindFirstChild("HumanoidRootPart")
        if not tRoot then break end

        angle += 30

        root.CFrame =
            CFrame.new(tRoot.Position + Vector3.new(0,1,0))
            * CFrame.Angles(math.rad(angle),0,0)

        root.Velocity = Vector3.new(9e8,9e8,9e8)
        root.RotVelocity = Vector3.new(9e8,9e8,9e8)

        task.wait()

    end

    bv:Destroy()

    hum:SetStateEnabled(Enum.HumanoidStateType.Seated,true)
    hum.PlatformStand = false

    root.CFrame = original

    cam.CameraSubject = hum

    for _,p in pairs(char:GetDescendants()) do
        if p:IsA("BasePart") then
            p.Velocity = Vector3.zero
            p.RotVelocity = Vector3.zero
        end
    end

    hum:UnequipTools()

    if toolRemote then
        toolRemote:InvokeServer("PickingTools","Couch")
    end

end

---------------------------------------------------
-- TOGGLE LOOP
---------------------------------------------------

Tab6:AddToggle({
    Name = "Fling sofa (Loop)",
    Description = "Fling repetidamente no player selecionado",
    Default = false,
    Callback = function(state)

        flingCrochActive = state

        if state then
            task.spawn(function()

                while flingCrochActive do
                    doFlingSofa()
                    task.wait(0.5)
                end

            end)
        end

    end
})

---------------------------------------------------
-- BOTÃƒO 1 VEZ
---------------------------------------------------

Tab6:AddButton({
    Name = "Fling sofa (1x)",
    Description = "Executa o fling apenas uma vez",
    Callback = function()
        doFlingSofa()
    end
})




Tab6:AddSection({"click"})

Tab6:AddButton({
Name = "Click Fling Doors",
Description = "Clique em um jogador para flingar ele usando portas",
Callback = function()

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local HRP = Character:WaitForChild("HumanoidRootPart")

local function isDoor(name)
name = string.lower(name)
return name:find("door") or name:find("doors") or name:find("porta")
end

-- BLACKHOLE
local BlackHole = Instance.new("Part")
BlackHole.Size = Vector3.new(100000,100000,100000)
BlackHole.Transparency = 1
BlackHole.Anchored = true
BlackHole.CanCollide = false
BlackHole.Name = "BlackHoleTarget"
BlackHole.Parent = Workspace

local baseAttachment = Instance.new("Attachment")
baseAttachment.Name = "BlackHoleAttachment"
baseAttachment.Parent = BlackHole

RunService.Heartbeat:Connect(function()
if HRP then
BlackHole.CFrame = HRP.CFrame
end
end)

-- LISTA DE PORTAS
local ControlledDoors = {}

-- NETWORK POWER
if not getgenv().Network then

getgenv().Network = {
BaseParts = {},
Velocity = Vector3.new(15,15,15)
}

Network.RetainPart = function(part)
if typeof(part) == "Instance"
and part:IsA("BasePart")
and part:IsDescendantOf(Workspace) then

table.insert(Network.BaseParts,part)
part.CustomPhysicalProperties = PhysicalProperties.new(0,0,0,0,0)
part.CanCollide = false

end
end

RunService.Heartbeat:Connect(function()

pcall(function()
sethiddenproperty(LocalPlayer,"SimulationRadius",math.huge)
end)

for _,part in pairs(Network.BaseParts) do
if part:IsDescendantOf(Workspace) then
part.Velocity = Network.Velocity
end
end

end)

end

-- PREPARAR PORTA
local function SetupPart(part)

if not part:IsA("BasePart") or part.Anchored or not isDoor(part.Name) then return end
if part:FindFirstChild("DoorAttached") then return end

part.CanCollide = false
Network.RetainPart(part)

for _,obj in ipairs(part:GetChildren()) do
if obj:IsA("AlignPosition") or obj:IsA("Torque") or obj:IsA("Attachment") then
obj:Destroy()
end
end

local marker = Instance.new("BoolValue")
marker.Name = "DoorAttached"
marker.Parent = part

local a1 = Instance.new("Attachment")
a1.Parent = part

local align = Instance.new("AlignPosition")
align.Parent = part
align.Attachment0 = a1
align.Attachment1 = baseAttachment
align.MaxForce = 1e20
align.MaxVelocity = math.huge
align.Responsiveness = 99999

local torque = Instance.new("Torque")
torque.Parent = part
torque.Attachment0 = a1
torque.RelativeTo = Enum.ActuatorRelativeTo.World
torque.Torque = Vector3.new(
math.random(-10e5,10e5)*90000,
math.random(-10e5,10e5)*90000,
math.random(-10e5,10e5)*90000
)

table.insert(ControlledDoors,{Part=part,Align=align})

end

-- DETECTAR PORTAS EXISTENTES
for _,obj in ipairs(Workspace:GetDescendants()) do
if obj:IsA("BasePart") and isDoor(obj.Name) then
SetupPart(obj)
end
end

-- NOVAS PORTAS
Workspace.DescendantAdded:Connect(function(obj)
if obj:IsA("BasePart") and isDoor(obj.Name) then
SetupPart(obj)
end
end)

-- PUXAR PORTAS
RunService.Heartbeat:Connect(function()

if HRP then

for _,part in pairs(Network.BaseParts) do
if part and part.Parent and isDoor(part.Name) then
part.Velocity = (HRP.Position - part.Position) * 60
end
end

end

end)

-- TOOL
local tool = Instance.new("Tool")
tool.Name = "Fling Porta"
tool.RequiresHandle = false
tool.Parent = LocalPlayer.Backpack

tool.Activated:Connect(function()

local mouse = LocalPlayer:GetMouse()
local target = mouse.Target
if not target then return end

local player = Players:GetPlayerFromCharacter(target:FindFirstAncestorOfClass("Model"))
if not player or player == LocalPlayer then return end

local char = player.Character
if not char then return end

local targetHRP = char:FindFirstChild("HumanoidRootPart")
if not targetHRP then return end

local targetAttachment = targetHRP:FindFirstChild("DoorTarget")

if not targetAttachment then
targetAttachment = Instance.new("Attachment")
targetAttachment.Name = "DoorTarget"
targetAttachment.Parent = targetHRP
end

for _,data in ipairs(ControlledDoors) do
if data.Align then
data.Align.Attachment1 = targetAttachment
end
end

task.wait(5)

for _,data in ipairs(ControlledDoors) do
if data.Align then
data.Align.Attachment1 = baseAttachment
end
end

end)

end
})



Tab6:AddSection({"Roubar itens dos players"})

-- SERVICES
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- VARIÃVEIS
local antiAll = false

--------------------------------------------------
-- FUNÃ‡ÃƒO REMOVER TOOL
--------------------------------------------------

local function removeTools(target)

    if target
    and target.Character then

        local tool = target.Character:FindFirstChildOfClass("Tool")

        if tool then

            local triggerRemote = ReplicatedStorage.RE:FindFirstChild("1Playe1rTrigge1rEven1t")
            local clearRemote = ReplicatedStorage.RE:FindFirstChild("1Clea1rTool1s")

            if triggerRemote and clearRemote then

                local args1 = {
                    [1] = "AcceptedToolToServer",
                    [2] = tool.Name,
                    [3] = target
                }

                triggerRemote:FireServer(unpack(args1))

                local args2 = {
                    [1] = "ClearAllTools"
                }

                clearRemote:FireServer(unpack(args2))

            end

        end

    end

end

--------------------------------------------------
-- ANTI TOOLS ALL (LOOP)
--------------------------------------------------

Tab6:AddToggle({
    Name = "rouba itens all",
    Default = false,
    Callback = function(v)

        antiAll = v

        if v then

            task.spawn(function()

                while antiAll do

                    for _,player in pairs(Players:GetPlayers()) do
                        if player ~= LocalPlayer then
                            removeTools(player)
                        end
                    end

                    task.wait(0.01)

                end

            end)

        end

    end
})

--------------------------------------------------
-- ROUBAR TOOLS ALL (1 VEZ)
--------------------------------------------------

Tab6:AddButton({
    Name = "Roubar itens ALL (1x)",
    Callback = function()

        for _,player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
                removeTools(player)
            end
        end

    end
})


Tab6:AddSection({"+18"})

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local plr = Players.LocalPlayer

--------------------------------------------------
-- VARIÃVEIS
--------------------------------------------------

local moveSpeed = 5
local speed = 10

local enabledFrontBack = false
local enabledGoGo = false

local connectionFront
local connectionGo

--------------------------------------------------
-- SLIDER VELOCIDADE FRENTE/TRÃS
--------------------------------------------------

Tab6:AddSlider({
    Name = "Speed de frente e trÃ¡s +18",
    Min = 1,
    Max = 100,
    Default = 5,
    Increment = 1,
    Callback = function(v)
        moveSpeed = v
    end
})

--------------------------------------------------
-- TOGGLE TELEPORT FRENTE/TRÃS
--------------------------------------------------

Tab6:AddToggle({
    Name = "Teleport Troll (On / Off)",
    Default = false,
    Callback = function(state)

        enabledFrontBack = state

        if connectionFront then
            connectionFront:Disconnect()
            connectionFront = nil
        end

        if not state or not selectedPlayer then return end

        local timeOffset = 0

        connectionFront = RunService.Heartbeat:Connect(function(dt)

            if not enabledFrontBack then return end

            local target = Players:FindFirstChild(selectedPlayer)
            if not target or not target.Character then return end

            local thrp = target.Character:FindFirstChild("HumanoidRootPart")

            local char = plr.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")

            if not (thrp and hrp) then return end

            timeOffset += dt * (moveSpeed * 0.3)

            local baseDistance = 4.5
            local oscillation = math.sin(timeOffset) * 3

            local verticalOffset = math.sin(timeOffset * 2) * 0.6

            local targetPos =
                thrp.Position
                - thrp.CFrame.LookVector * (baseDistance + oscillation)
                + Vector3.new(0, verticalOffset, 0)

            local shake = Vector3.new(
                math.random(-10,10) / 70,
                math.random(-10,10) / 70,
                0
            )

            hrp.CFrame = CFrame.lookAt(
                targetPos + shake,
                thrp.Position
            )

        end)

    end
})




--------------------------------------------------
-- SLIDER VELOCIDADE GOGOGO
--------------------------------------------------

Tab6:AddSlider({
    Name = "Speed do gogogogo",
    Min = 1,
    Max = 100,
    Default = 10,
    Increment = 1,
    Callback = function(v)
        speed = v
    end
})

--------------------------------------------------
-- TOGGLE GOGOGO PLAYER
--------------------------------------------------

Tab6:AddToggle({
    Name = "gogogogo no player +18",
    Default = false,
    Callback = function(state)

        enabledGoGo = state

        if connectionGo then
            connectionGo:Disconnect()
            connectionGo = nil
        end

        if not state or not selectedPlayer then return end

        local timeValue = 0

        connectionGo = RunService.Heartbeat:Connect(function(dt)

            if not enabledGoGo then return end

            local target = Players:FindFirstChild(selectedPlayer)
            if not target or not target.Character then return end

            local tChar = target.Character
            local tRoot = tChar:FindFirstChild("HumanoidRootPart")
            local tHead = tChar:FindFirstChild("Head")

            local char = plr.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")

            if not (tRoot and tHead and hrp) then return end

            local myHipHeight = hrp.Size.Y * 0.5

            local heightOffset = Vector3.new(
                0,
                tHead.Size.Y * 0.4 + myHipHeight * 0.15,
                0
            )

            timeValue += dt * speed

            local backAndForth = math.sin(timeValue) * 2.5

            local finalPosition =
                tRoot.Position
                + tRoot.CFrame.LookVector * (3 + backAndForth)
                + heightOffset

            hrp.CFrame = CFrame.lookAt(finalPosition, tHead.Position)

        end)

    end
})



--====================================================
-- PLACAS TROLL SPECTRA (VERSÃƒO FINAL)
--====================================================

Tab6:AddSection({"troll Outdoor | Billboard Spectra"})

local mensagemSelecionada = "<<|{ã€Ž SPECTRA invadiu o servidor hahahahahahha ã€}|>>"
local mensagemCustom = ""

--====================================================
-- DROPDOWN (SUAS MENSAGENS)
--====================================================
Tab6:AddDropdown({
    Name = "Mensagem",
    Options = {
        "Spectra invade",
        "Sistema invadido",
        "VocÃª Ã© meu",
        "Xiu beta",
        "RP morte",
        "Spectra domina",
        "VocÃª nÃ£o escapa",
        "Controle total",
        "Fim do server",
        "Spectra chegou"
    },
    Default = "Spectra invade",
    Callback = function(v)

        if v == "Spectra invade" then
            mensagemSelecionada = "<<|{ã€Ž SPECTRA invadiu o servidor hahahahahahha ã€}|>>"

        elseif v == "Sistema invadido" then
            mensagemSelecionada = "<<|{ã€Ž sistema invadido ã€}|>>"

        elseif v == "VocÃª Ã© meu" then
            mensagemSelecionada = "<<|{ã€Ž vocÃª Ã© meu ã€}|>>"

        elseif v == "Xiu beta" then
            mensagemSelecionada = "<<|{ã€Ž xiu beta ã€}|>>"

        elseif v == "RP morte" then
            mensagemSelecionada = "<<|{ã€Ž jogando RP vocÃª vai morrer ã€}|>>"

        elseif v == "Spectra domina" then
            mensagemSelecionada = "<<|{ã€Ž SPECTRA vai dominar tudo ã€}|>>"

        elseif v == "VocÃª nÃ£o escapa" then
            mensagemSelecionada = "<<|{ã€Ž vocÃª nÃ£o escapa ã€}|>>"

        elseif v == "Controle total" then
            mensagemSelecionada = "<<|{ã€Ž controle total do servidor ã€}|>>"

        elseif v == "Fim do server" then
            mensagemSelecionada = "<<|{ã€Ž fim do servidor ã€}|>>"

        elseif v == "Spectra chegou" then
            mensagemSelecionada = "<<|{ã€Ž SPECTRA chegou  ã€}|>>"
        end

    end
})

--====================================================
-- INPUT (NÃƒO QUEBRA SE NÃƒO EXISTIR)
--====================================================
pcall(function()
    Tab6:AddInput({
        Name = "Mensagem custom",
        Placeholder = "Digite...",
        Callback = function(text)
            mensagemCustom = text
        end
    })
end)

--====================================================
-- BOTÃƒO PRINCIPAL
--====================================================
Tab6:AddButton({
    Name = "mudar Outdoor | Billboard",
    Callback = function()

        local plr = game.Players.LocalPlayer
        local char = plr.Character
        if not char then return end

        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        local texto = (mensagemCustom ~= "" and mensagemCustom) or mensagemSelecionada
        local old = hrp.CFrame

        local function safeTP(v)
            pcall(function()
                hrp.CFrame = CFrame.new(v)
            end)
        end

        local function safeFire(slot)
            pcall(function()
                game:GetService("ReplicatedStorage")
                :WaitForChild("RE")
                :WaitForChild("1Cemeter1y")
                :FireServer("ReturningCommercialWords", slot, nil, texto)
            end)
        end

        -- 1Âª placa
        safeTP(Vector3.new(-236.72, 88.21, -547.19))
        task.wait(0.7)
        safeFire(3)

        -- 2Âª placa
        safeTP(Vector3.new(-636.46, 25.11, 359.66))
        task.wait(0.7)
        safeFire(2)

        -- 3Âª placa
        safeTP(Vector3.new(359.74, 56.86, 582.29))
        task.wait(0.7)
        safeFire(1)

        -- voltar
        task.wait(0.5)
        hrp.CFrame = old
    end
})







local selectedPlayer

local function getPlayers()
    local t = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= plr then
            table.insert(t, p.Name)
        end
    end
    return t
end

local dropdown = PropsTab:AddDropdown({
    Name = "Escolher Player",
    Options = getPlayers(),
    Callback = function(v)
        selectedPlayer = v
    end
})

PropsTab:AddButton({
    Name = "Atualizar Lista",
    Callback = function()
        dropdown:Set(getPlayers())
    end
})






local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

local spectating = false
local spectateConn
local charConn
local currentTarget


local function resetCamera()
if spectateConn then spectateConn:Disconnect() spectateConn = nil end
if charConn then charConn:Disconnect() charConn = nil end

currentTarget = nil

local char = LocalPlayer.Character
local hum = char and char:FindFirstChildOfClass("Humanoid")
if hum then
Camera.CameraSubject = hum
Camera.CameraType = Enum.CameraType.Custom
end

end

local function spectatePlayer(player)
if not spectating or not player then return end

resetCamera()
spectating = true
currentTarget = player

spectateConn = RunService.RenderStepped:Connect(function()
if not spectating then return end
if not currentTarget or not currentTarget.Character then return end

local hum = currentTarget.Character:FindFirstChildOfClass("Humanoid")
if hum then
Camera.CameraSubject = hum
Camera.CameraType = Enum.CameraType.Custom
end

end)

charConn = player.CharacterAdded:Connect(function()
task.wait(0.1)
if spectating and currentTarget == player then
spectatePlayer(player)
end
end)

end


Players.PlayerRemoving:Connect(function(plr)
if spectating and currentTarget == plr then
resetCamera()
spectating = false
end
end)


PropsTab:AddToggle({
Name = "Spectar Jogador",
Default = false,
Callback = function(state)
spectating = state

if not state then
resetCamera()
return
end

local target = Players:FindFirstChild(selectedPlayer)
if target then
spectatePlayer(target)
else
resetCamera()
end

end

})


PropsTab:AddSection({"props troll ALL"})





local PROP_RAID_ENABLED = false
local PROP_RAID_CONNECTION
local PROP_RAID_Targets = {}

local function UpdateRaidCharacter(char)
Character = char
HRP = char:WaitForChild("HumanoidRootPart", 5)
Humanoid = char:WaitForChild("Humanoid", 5)
end

if LocalPlayer.Character then
UpdateRaidCharacter(LocalPlayer.Character)
end

LocalPlayer.CharacterAdded:Connect(function(char)
StopPropRaid()
task.wait(0.5)
UpdateRaidCharacter(char)
if PROP_RAID_ENABLED then
task.wait(0.5)
StartPropRaid()
end
end)

pcall(function()
ReplicatedStorage.RE["1Too1l"]:InvokeServer("PickingTools", "PropMaker")
end)

task.wait(0.3)

pcall(function()
ReplicatedStorage.RE["1Clea1rTool1s"]:FireServer(
"RequestingPropName",
"FurnitureBleachers",
"Furniture"
)
end)

local propCache = {}
local lastUpdate = 0

local function GetMyPropsRaid()
if tick() - lastUpdate < 0.4 then return propCache end

propCache = {}
local ws = workspace:FindFirstChild("WorkspaceCom")
if not ws then return propCache end

for _, folder in ipairs(ws:GetChildren()) do
    for _, prop in ipairs(folder:GetChildren()) do
        if prop:FindFirstChild("SetCurrentCFrame")
        and prop.Name:lower():find(LocalPlayer.Name:lower()) then
            table.insert(propCache, prop)
        end
    end
end

lastUpdate = tick()
return propCache

end

local function GetTargetsRaid()
local t = {}
if not HRP then return t end

for _, plr in ipairs(Players:GetPlayers()) do
    if plr ~= LocalPlayer
    and plr.Character
    and plr.Character:FindFirstChild("HumanoidRootPart") then
        local dist = (plr.Character.HumanoidRootPart.Position - HRP.Position).Magnitude
        if dist <= 222 then
            table.insert(t, plr)
        end
    end
end
return t

end

local function AssignTargetsRaid(props, targets)
if #targets == 0 then return end
for i, prop in ipairs(props) do
local index = ((i - 1) % #targets) + 1
PROP_RAID_Targets[prop] = targets[index]
end
end

local function GetNextTargetRaid(targets, current)
if #targets == 0 then return nil end
for i, plr in ipairs(targets) do
if plr == current then
return targets[(i % #targets) + 1]
end
end
return targets[1]
end

local angle = 0
local height = 0
local dir = 1

function StartPropRaid()
if PROP_RAID_CONNECTION then PROP_RAID_CONNECTION:Disconnect() end

PROP_RAID_CONNECTION = RunService.Heartbeat:Connect(function()
    if not Character or not HRP or not Humanoid or Humanoid.Health <= 0 then return end

    local props = GetMyPropsRaid()
    local targets = GetTargetsRaid()
    if #props == 0 or #targets == 0 then return end

    AssignTargetsRaid(props, targets)

    angle += math.rad(20)
    if height > 4 then dir = -1 end
    if height < -4 then dir = 1 end
    height += 0.3 * dir

    for _, prop in ipairs(props) do
        local target = PROP_RAID_Targets[prop]
        if not target or not target.Character then
            PROP_RAID_Targets[prop] = GetNextTargetRaid(targets, target)
            continue
        end

        local hum = target.Character:FindFirstChild("Humanoid")
        local root = target.Character:FindFirstChild("HumanoidRootPart")
        if not hum or not root then continue end

        if hum.Sit then
            PROP_RAID_Targets[prop] = GetNextTargetRaid(targets, target)
            continue
        end

        local radius = math.random(2,5)
        local offset = Vector3.new(math.cos(angle)*radius,height,math.sin(angle)*radius)

        pcall(function()
            prop.SetCurrentCFrame:InvokeServer(
                root.CFrame * CFrame.new(offset)
            )
        end)
    end
end)

end

function StopPropRaid()
if PROP_RAID_CONNECTION then
PROP_RAID_CONNECTION:Disconnect()
PROP_RAID_CONNECTION = nil
end
PROP_RAID_Targets = {}
end

PropsTab:AddToggle({
Name = "prÃ³p sit all",
Default = false,
Callback = function(v)
PROP_RAID_ENABLED = v
if v then
StartPropRaid()
else
StopPropRaid()
end
end
})





local HUNT_ENABLED = false
local HUNT_CONNECTION
local TARGET_RADIUS = 150
local MAX_PROPS_PER_PLAYER = 3

local PropTargetMap = {}
local DroppedProps = {}

local function GetNearbyPlayers()
    local list = {}
    if not HRP then return list end

    for _,p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer
        and p.Character
        and p.Character:FindFirstChild("HumanoidRootPart")
        and p.Character:FindFirstChild("Humanoid")
        and p.Character.Humanoid.Health > 0 then

            local dist = (p.Character.HumanoidRootPart.Position - HRP.Position).Magnitude
            if dist <= TARGET_RADIUS then
                table.insert(list, p)
            end
        end
    end

    return list
end

local function CountPropsOnPlayer(player)
    local count = 0
    for _,target in pairs(PropTargetMap) do
        if target == player then
            count += 1
        end
    end
    return count
end

local function GetBestTarget(targets)
    local bestPlayer = nil
    local lowestCount = math.huge

    for _,plr in ipairs(targets) do
        local c = CountPropsOnPlayer(plr)
        if c < lowestCount and c < MAX_PROPS_PER_PLAYER then
            lowestCount = c
            bestPlayer = plr
        end
    end

    return bestPlayer
end

local function StartHunter()
    if HUNT_CONNECTION then HUNT_CONNECTION:Disconnect() end

    local t = 0

    HUNT_CONNECTION = RunService.Heartbeat:Connect(function(dt)
        if not Character or not HRP or not Humanoid or Humanoid.Health <= 0 then return end
        t += dt

        local props = GetMyPropsRaid()
        if #props == 0 then return end

        local targets = GetNearbyPlayers()

        for i, prop in ipairs(props) do
            local currentTarget = PropTargetMap[prop]

            -- verifica se alvo saiu da Ã¡rea ou morreu
            if currentTarget then
                local char = currentTarget.Character
                if not char
                or not char:FindFirstChild("HumanoidRootPart")
                or not char:FindFirstChild("Humanoid")
                or char.Humanoid.Health <= 0
                or (char.HumanoidRootPart.Position - HRP.Position).Magnitude > TARGET_RADIUS then
                    PropTargetMap[prop] = nil
                    currentTarget = nil
                end
            end

            -- escolhe novo alvo inteligente
            if not currentTarget then
                local newTarget = GetBestTarget(targets)
                PropTargetMap[prop] = newTarget
                currentTarget = newTarget
            end

            -- sem ninguÃ©m perto â†’ volta pra vocÃª
            if not currentTarget then
                local offset = Vector3.new(
                    math.cos(i+t)*4,
                    2 + math.sin(t*2+i),
                    math.sin(i+t)*4
                )

                local cf = CFrame.new(HRP.Position + offset)

                pcall(function()
                    prop.SetCurrentCFrame:InvokeServer(
                        prop:GetPivot():Lerp(cf, 0.3)
                    )
                end)

                continue
            end

            local hum = currentTarget.Character:FindFirstChild("Humanoid")
            local root = currentTarget.Character:FindFirstChild("HumanoidRootPart")
            if not hum or not root then continue end

            -- sentou â†’ joga pra baixo
            if hum.Sit then
                if not DroppedProps[prop] then
                    DroppedProps[prop] = true

                    local dropPos = Vector3.new(
                        root.Position.X,
                        -460,
                        root.Position.Z
                    )

                    pcall(function()
                        prop.SetCurrentCFrame:InvokeServer(CFrame.new(dropPos))
                    end)
                end
                continue
            end

            -- levantou â†’ volta pro sistema
            if DroppedProps[prop] and not hum.Sit then
                DroppedProps[prop] = nil
            end

            -- movimento inteligente perto do player (baixo + mexendo)
            local move = Vector3.new(
                math.cos(t*3 + i) * 2,
                -1.5 + math.sin(t*4 + i),
                math.sin(t*3 + i) * 2
            )

            local cf = root.CFrame * CFrame.new(move) *
                CFrame.Angles(
                    math.rad(math.sin(t*5+i)*120),
                    math.rad(t*180),
                    0
                )

            pcall(function()
                prop.SetCurrentCFrame:InvokeServer(
                    prop:GetPivot():Lerp(cf, 0.35)
                )
            end)
        end
    end)
end

local function StopHunter()
    if HUNT_CONNECTION then
        HUNT_CONNECTION:Disconnect()
        HUNT_CONNECTION = nil
    end
    PropTargetMap = {}
    DroppedProps = {}
end

PropsTab:AddToggle({
    Name = "prop kill all",
    Default = false,
    Callback = function(v)
        HUNT_ENABLED = v
        if v then
            StartHunter()
        else
            StopHunter()
        end
    end
})




PropsTab:AddSection({"props para usar em você"})







local COBRA_ENABLED = false
local COBRA_CONNECTION

local FOLLOW_DISTANCE = 3.5
local SIDE_AMPLITUDE = 3.2
local UP_AMPLITUDE = 1.2
local WAVE_SPEED = 10
local PHASE_OFFSET = 1.1
local SMOOTHNESS = 0.35

function StartCobra()
if COBRA_CONNECTION then COBRA_CONNECTION:Disconnect() end
local time = 0

COBRA_CONNECTION = RunService.Heartbeat:Connect(function(dt)
    if not Character or not HRP or not Humanoid or Humanoid.Health <= 0 then return end

    local props = GetMyPropsRaid()
    if #props == 0 then return end

    time += dt * WAVE_SPEED

    for i, prop in ipairs(props) do
        local phase = time - i * PHASE_OFFSET

        local backDir = -HRP.CFrame.LookVector
        local rightDir = HRP.CFrame.RightVector
        local upDir = Vector3.new(0,1,0)

        local backOffset = backDir * (FOLLOW_DISTANCE * i)
        local sideOffset = rightDir * (math.sin(phase) * SIDE_AMPLITUDE)
        local upOffset = upDir * (math.cos(phase * 0.8) * UP_AMPLITUDE)

        local pos = HRP.Position + backOffset + sideOffset + upOffset
        local cf = CFrame.new(pos, pos + backDir)

        pcall(function()
            prop.SetCurrentCFrame:InvokeServer(
                prop:GetPivot():Lerp(cf, SMOOTHNESS)
            )
        end)
    end
end)

end

function StopCobra()
if COBRA_CONNECTION then
COBRA_CONNECTION:Disconnect()
COBRA_CONNECTION = nil
end
end

PropsTab:AddToggle({
Name = "Prop Cobra",
Default = false,
Callback = function(v)
COBRA_ENABLED = v
if v then
StartCobra()
else
StopCobra()
end
end
})




local FURACAO_ENABLED = false
local FURACAO_CONNECTION

local function StartFuracao()
    if FURACAO_CONNECTION then FURACAO_CONNECTION:Disconnect() end

    local t = 0

    FURACAO_CONNECTION = RunService.Heartbeat:Connect(function(dt)
        if not Character or not HRP or not Humanoid or Humanoid.Health <= 0 then return end

        local props = GetMyPropsRaid()
        if #props == 0 then return end

        t += dt * 4

        for i, prop in ipairs(props) do
            local angle = t + (i * 0.7)

            -- distÃ¢ncia varia (afasta e aproxima)
            local radius = 4 + math.sin(t + i) * 3

            -- sobe e desce
            local height = math.cos(t*2 + i) * 4

            -- gira em volta
            local offset = Vector3.new(
                math.cos(angle) * radius,
                height,
                math.sin(angle) * radius
            )

            local pos = HRP.Position + offset
            local look = CFrame.new(pos, HRP.Position)

            pcall(function()
                prop.SetCurrentCFrame:InvokeServer(
                    prop:GetPivot():Lerp(look, 0.35)
                )
            end)
        end
    end)
end

local function StopFuracao()
    if FURACAO_CONNECTION then
        FURACAO_CONNECTION:Disconnect()
        FURACAO_CONNECTION = nil
    end
end

PropsTab:AddToggle({
    Name = "FuracÃ£o de Props",
    Default = false,
    Callback = function(v)
        FURACAO_ENABLED = v
        if v then
            StartFuracao()
        else
            StopFuracao()
        end
    end
})




local AIR_ENABLED = false
local AIR_CONNECTION

local function StartAirSpread()
    if AIR_CONNECTION then AIR_CONNECTION:Disconnect() end

    local t = 0

    AIR_CONNECTION = RunService.Heartbeat:Connect(function(dt)
        if not Character or not HRP or not Humanoid or Humanoid.Health <= 0 then return end

        local props = GetMyPropsRaid()
        if #props == 0 then return end

        t += dt

        for i, prop in ipairs(props) do
            -- ALTURA fixa ~25m
            local height = 30 + math.sin(t + i) * 3

            -- ESPALHAMENTO MUITO MAIOR
            local angle = (i * 2.5)
            local radius = 12 + (i * 2)   -- bem mais longe um do outro

            local offset = Vector3.new(
                math.cos(angle) * radius,
                height,
                math.sin(angle) * radius
            )

            local pos = HRP.Position + offset

            local cf = CFrame.new(pos) * CFrame.Angles(
                math.rad((t*50 + i*30) % 360),
                math.rad((t*35 + i*20) % 360),
                0
            )

            pcall(function()
                prop.SetCurrentCFrame:InvokeServer(
                    prop:GetPivot():Lerp(cf, 0.30)
                )
            end)
        end
    end)
end

local function StopAirSpread()
    if AIR_CONNECTION then
        AIR_CONNECTION:Disconnect()
        AIR_CONNECTION = nil
    end
end

PropsTab:AddToggle({
    Name = "Props strela",
    Default = false,
    Callback = function(v)
        AIR_ENABLED = v
        if v then
            StartAirSpread()
        else
            StopAirSpread()
        end
    end
})






local FRONT_CIRCLE_ENABLED = false
local FRONT_CIRCLE_CONNECTION

local function StartFrontCircle()
    if FRONT_CIRCLE_CONNECTION then FRONT_CIRCLE_CONNECTION:Disconnect() end

    local t = 0

    FRONT_CIRCLE_CONNECTION = RunService.Heartbeat:Connect(function(dt)
        if not Character or not HRP or not Humanoid or Humanoid.Health <= 0 then return end

        local props = GetMyPropsRaid()
        if #props == 0 then return end

        t += dt * 1.8

        -- posiÃ§Ã£o do "portal" na frente
        local center = HRP.Position + HRP.CFrame.LookVector * 8 + Vector3.new(0, 2, 0)

        local total = #props
        local radius = 6 -- MAIS ESPAÃ‡ADO

        for i, prop in ipairs(props) do
            local angle = ((i-1)/total) * math.pi * 2
            local spin = t

            -- cÃ­rculo VERTICAL (altura + lateral)
            local side = math.cos(angle + spin) * radius
            local up = math.sin(angle + spin) * radius

            local pos =
                center
                + HRP.CFrame.RightVector * side
                + Vector3.new(0, up, 0)

            local cf = CFrame.new(pos) * CFrame.Angles(
                math.rad(up * 5),
                math.rad((angle+spin)*180/math.pi),
                0
            )

            pcall(function()
                prop.SetCurrentCFrame:InvokeServer(
                    prop:GetPivot():Lerp(cf, 0.25)
                )
            end)
        end
    end)
end

local function StopFrontCircle()
    if FRONT_CIRCLE_CONNECTION then
        FRONT_CIRCLE_CONNECTION:Disconnect()
        FRONT_CIRCLE_CONNECTION = nil
    end
end

PropsTab:AddToggle({
    Name = "Portal prop",
    Default = false,
    Callback = function(v)
        FRONT_CIRCLE_ENABLED = v
        if v then
            StartFrontCircle()
        else
            StopFrontCircle()
        end
    end
})




local CRAZY_ME_CONNECTION
local STATES = {}

local MAX_DIST = 20
local CHANGE_TIME_MIN = 0.15
local CHANGE_TIME_MAX = 0.45

local function newState(prop)
    STATES[prop] = {
        nextChange = tick() + math.random() * 0.3,
        targetOffset = Vector3.new(
            math.random(-MAX_DIST, MAX_DIST),
            math.random(-15, 20),
            math.random(-MAX_DIST, MAX_DIST)
        ),
        rotSpeed = Vector3.new(
            math.random(200,900),
            math.random(200,900),
            math.random(200,900)
        )
    }
end

local function StartCrazyMe()
    if CRAZY_ME_CONNECTION then CRAZY_ME_CONNECTION:Disconnect() end

    CRAZY_ME_CONNECTION = RunService.Heartbeat:Connect(function()
        if not character or not character:FindFirstChild("HumanoidRootPart") then return end
        local root = character.HumanoidRootPart

        local props = GetMyPropsRaid()
        if #props == 0 then return end

        for _,prop in ipairs(props) do
            if not STATES[prop] then
                newState(prop)
            end

            local st = STATES[prop]

            -- muda posição direto (sem sincronia)
            if tick() >= st.nextChange then
                st.targetOffset = Vector3.new(
                    math.random(-MAX_DIST, MAX_DIST),
                    math.random(-15, 20),
                    math.random(-MAX_DIST, MAX_DIST)
                )

                st.rotSpeed = Vector3.new(
                    math.random(300,1200),
                    math.random(300,1200),
                    math.random(300,1200)
                )

                st.nextChange = tick() + math.random(CHANGE_TIME_MIN*100, CHANGE_TIME_MAX*100)/100
            end

            local targetPos = root.Position + st.targetOffset

            local rx = math.rad((tick()*st.rotSpeed.X)%360)
            local ry = math.rad((tick()*st.rotSpeed.Y)%360)
            local rz = math.rad((tick()*st.rotSpeed.Z)%360)

            pcall(function()
                prop.SetCurrentCFrame:InvokeServer(
                    prop:GetPivot():Lerp(
                        CFrame.new(targetPos) * CFrame.Angles(rx,ry,rz),
                        0.35
                    )
                )
            end)
        end
    end)
end

local function StopCrazyMe()
    if CRAZY_ME_CONNECTION then
        CRAZY_ME_CONNECTION:Disconnect()
        CRAZY_ME_CONNECTION = nil
    end
    STATES = {}
end

PropsTab:AddToggle({
    Name = "Props Doidos",
    Default = false,
    Callback = function(v)
        if v then
            StartCrazyMe()
        else
            StopCrazyMe()
        end
    end
})


PropsTab:AddSection({"props para usar em pessoas que estão selecionadas na lista"})






local TARGET_ENABLED = false
local TARGET_CONNECTION
local SentProps = {}

local function getTargetPlayer()
    if not selectedPlayer then return nil end
    return Players:FindFirstChild(selectedPlayer)
end

local function StartSelectedTarget()
    if TARGET_CONNECTION then TARGET_CONNECTION:Disconnect() end

    local t = 0

    TARGET_CONNECTION = RunService.Heartbeat:Connect(function(dt)
        if not Character or not HRP or not Humanoid or Humanoid.Health <= 0 then return end

        local target = getTargetPlayer()
        if not target or not target.Character then return end

        local root = target.Character:FindFirstChild("HumanoidRootPart")
        local hum = target.Character:FindFirstChild("Humanoid")
        if not root or not hum then return end

        local props = GetMyPropsRaid()
        if #props == 0 then return end

        t += dt * 3

        for i, prop in ipairs(props) do

            -- SENTOU â†’ sobe MUITO ALTO
            if hum.Sit then
                if not SentProps[prop] then
                    SentProps[prop] = true

                    local upPos = Vector3.new(
                        root.Position.X,
                        1000000, -- muito alto
                        root.Position.Z
                    )

                    pcall(function()
                        prop.SetCurrentCFrame:InvokeServer(CFrame.new(upPos))
                    end)
                end
                continue
            end

            -- LEVANTOU â†’ volta pra vocÃª
            if SentProps[prop] then
                SentProps[prop] = nil

                local backPos = HRP.Position + Vector3.new(
                    math.cos(i)*4,
                    3,
                    math.sin(i)*4
                )

                pcall(function()
                    prop.SetCurrentCFrame:InvokeServer(CFrame.new(backPos))
                end)
                continue
            end


            local move = Vector3.new(
                math.cos(t + i) * 3,
                math.sin(t*2 + i) * 2,
                math.sin(t + i*2) * 3
            )

            local cf = root.CFrame * CFrame.new(move) *
                CFrame.Angles(
                    math.rad(math.sin(t*4+i)*180),
                    math.rad(t*200),
                    math.rad(math.cos(t*3+i)*180)
                )

            pcall(function()
                prop.SetCurrentCFrame:InvokeServer(
                    prop:GetPivot():Lerp(cf, 0.35)
                )
            end)
        end
    end)
end

local function StopSelectedTarget()
    if TARGET_CONNECTION then
        TARGET_CONNECTION:Disconnect()
        TARGET_CONNECTION = nil
    end
    SentProps = {}
end

PropsTab:AddToggle({
    Name = "próp fling",
    Default = false,
    Callback = function(v)
        TARGET_ENABLED = v
        if v then
            StartSelectedTarget()
        else
            StopSelectedTarget()
        end
    end
})




local TARGET_ENABLED = false
local TARGET_CONNECTION
local SentProps = {}

local function getTargetPlayer()
    if not selectedPlayer then return nil end
    return Players:FindFirstChild(selectedPlayer)
end

local function StartSelectedTarget()
    if TARGET_CONNECTION then TARGET_CONNECTION:Disconnect() end

    local t = 0

    TARGET_CONNECTION = RunService.Heartbeat:Connect(function(dt)
        if not Character or not HRP or not Humanoid or Humanoid.Health <= 0 then return end

        local target = getTargetPlayer()
        if not target or not target.Character then return end

        local root = target.Character:FindFirstChild("HumanoidRootPart")
        local hum = target.Character:FindFirstChild("Humanoid")
        if not root or not hum then return end

        local props = GetMyPropsRaid()
        if #props == 0 then return end

        t += dt * 3

        for i, prop in ipairs(props) do

            -- SENTOU â†’ vai pra -460
            if hum.Sit then
                if not SentProps[prop] then
                    SentProps[prop] = true

                    local downPos = Vector3.new(
                        root.Position.X,
                        -460,
                        root.Position.Z
                    )

                    pcall(function()
                        prop.SetCurrentCFrame:InvokeServer(CFrame.new(downPos))
                    end)
                end
                continue
            end

 
            if SentProps[prop] then
                SentProps[prop] = nil

                local backPos = HRP.Position + Vector3.new(
                    math.cos(i)*4,
                    3,
                    math.sin(i)*4
                )

                pcall(function()
                    prop.SetCurrentCFrame:InvokeServer(CFrame.new(backPos))
                end)
                continue
            end


            local move = Vector3.new(
                math.cos(t + i) * 3,
                math.sin(t*2 + i) * 2,
                math.sin(t + i*2) * 3
            )

            local cf = root.CFrame * CFrame.new(move) *
                CFrame.Angles(
                    math.rad(math.sin(t*4+i)*180),
                    math.rad(t*200),
                    math.rad(math.cos(t*3+i)*180)
                )

            pcall(function()
                prop.SetCurrentCFrame:InvokeServer(
                    prop:GetPivot():Lerp(cf, 0.35)
                )
            end)
        end
    end)
end

local function StopSelectedTarget()
    if TARGET_CONNECTION then
        TARGET_CONNECTION:Disconnect()
        TARGET_CONNECTION = nil
    end
    SentProps = {}
end

PropsTab:AddToggle({
    Name = "próp kill player",
    Default = false,
    Callback = function(v)
        TARGET_ENABLED = v
        if v then
            StartSelectedTarget()
        else
            StopSelectedTarget()
        end
    end
})



local RING_ENABLED = false
local RING_CONNECTION

local function getTargetPlayer()
    if not selectedPlayer then return nil end
    return Players:FindFirstChild(selectedPlayer)
end

local function StartRing()
    if RING_CONNECTION then RING_CONNECTION:Disconnect() end

    local t = 0

    RING_CONNECTION = RunService.Heartbeat:Connect(function(dt)
        if not Character or not HRP or not Humanoid or Humanoid.Health <= 0 then return end

        local target = getTargetPlayer()
        if not target or not target.Character then return end

        local root = target.Character:FindFirstChild("HumanoidRootPart")
        if not root then return end

        local props = GetMyPropsRaid()
        if #props == 0 then return end

        t += dt * 1.8

        local radius = 8
        local total = #props

        -- tamanho do buraco em graus (ex: 70Â° vazio)
        local gapAngle = math.rad(70)
        local usableAngle = (math.pi * 2) - gapAngle

        for i, prop in ipairs(props) do
            -- distribui TODOS dentro da parte que sobra do cÃ­rculo
            local percent = (i-1) / total
            local angle = percent * usableAngle

            local spin = t
            local finalAngle = angle + spin

            local x = math.cos(finalAngle) * radius
            local z = math.sin(finalAngle) * radius

            local pos = root.Position + Vector3.new(x, -2.7, z)

            local cf = CFrame.new(pos) * CFrame.Angles(
                0,
                math.rad((finalAngle) * 180/math.pi),
                0
            )

            pcall(function()
                prop.SetCurrentCFrame:InvokeServer(
                    prop:GetPivot():Lerp(cf, 0.28)
                )
            end)
        end
    end)
end

local function StopRing()
    if RING_CONNECTION then
        RING_CONNECTION:Disconnect()
        RING_CONNECTION = nil
    end
end

PropsTab:AddToggle({
    Name = "próp círculo",
    Default = false,
    Callback = function(v)
        RING_ENABLED = v
        if v then
            StartRing()
        else
            StopRing()
        end
    end
})



local COBRA_CONNECTION
local COBRA_ACTIVE = false

local FOLLOW_DISTANCE = 3.5
local SIDE_AMPLITUDE = 3.2
local UP_AMPLITUDE = 1.2
local WAVE_SPEED = 10
local PHASE_OFFSET = 1.1
local SMOOTHNESS = 0.35

local function getTargetPlayer()
    if not selectedPlayer then return nil end
    return Players:FindFirstChild(selectedPlayer)
end

local function stopAllPropModes()
    if COBRA_CONNECTION then
        COBRA_CONNECTION:Disconnect()
        COBRA_CONNECTION = nil
    end
end

local function StartCobraTarget()
    stopAllPropModes()

    local t = 0

    COBRA_CONNECTION = RunService.Heartbeat:Connect(function(dt)
        local target = getTargetPlayer()
        if not target or not target.Character then return end

        local root = target.Character:FindFirstChild("HumanoidRootPart")
        local hum = target.Character:FindFirstChild("Humanoid")
        if not root or not hum or hum.Health <= 0 then return end

        local props = GetMyPropsRaid()
        if #props == 0 then return end

        t += dt * WAVE_SPEED

        for i, prop in ipairs(props) do
            local phase = t - i * PHASE_OFFSET

            local backDir = -root.CFrame.LookVector
            local rightDir = root.CFrame.RightVector
            local upDir = Vector3.new(0,1,0)

            local backOffset = backDir * (FOLLOW_DISTANCE * i)
            local sideOffset = rightDir * (math.sin(phase) * SIDE_AMPLITUDE)
            local upOffset = upDir * (math.cos(phase * 0.8) * UP_AMPLITUDE)

            local pos = root.Position + backOffset + sideOffset + upOffset
            local cf = CFrame.new(pos, pos + backDir)

            pcall(function()
                prop.SetCurrentCFrame:InvokeServer(
                    prop:GetPivot():Lerp(cf, SMOOTHNESS)
                )
            end)
        end
    end)
end

local function StopCobraTarget()
    if COBRA_CONNECTION then
        COBRA_CONNECTION:Disconnect()
        COBRA_CONNECTION = nil
    end
end

PropsTab:AddToggle({
    Name = "Prop Cobra em player",
    Default = false,
    Callback = function(v)
        COBRA_ACTIVE = v
        if v then
            StartCobraTarget()
        else
            StopCobraTarget()
        end
    end
})









--////////////////////////////////////////////////////
-- VARIÁVEIS

local loopAtivo = false
local loopSomFixo = false
local InputID = ""

local tocarLocal = true -- true = escuta / false = não escuta

local somFixoID = 6314880174

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local RE = ReplicatedStorage:WaitForChild("RE")
local GunRemote = RE:WaitForChild("1Gu1n")
local SoundRemote = RE:WaitForChild("1Gu1nSound1s")

--////////////////////////////////////////////////////
-- INPUT

Tab7:AddTextBox({
    Name = "ID do Áudio",
    Description = "Digite o ID do som",
    Default = "",
    PlaceholderText = "Ex: 6832470734",
    ClearTextOnFocus = true,
    Callback = function(text)
        InputID = tonumber(text)
    end
})

--////////////////////////////////////////////////////
-- CONFIG

Tab7:AddToggle({
    Name = "Não tocar em mim",
    Description = "Você não escuta o som",
    Default = false,
    Callback = function(state)
        tocarLocal = not state
    end
})

--////////////////////////////////////////////////////
-- FUNÇÃO GLOBAL (CORRIGIDA)

local function PlayGlobal(ID)
    local tocouUmaVez = false

    for _, plr in ipairs(Players:GetPlayers()) do
        if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            
            local hrp = plr.Character.HumanoidRootPart

            --------------------------------------------------
            -- VOCÊ (SOM CONTROLADO)
            --------------------------------------------------
            if plr == LocalPlayer then
                if tocarLocal then
                    local sound = Instance.new("Sound")
                    sound.SoundId = "rbxassetid://" .. ID
                    sound.Volume = 2
                    sound.RollOffMode = Enum.RollOffMode.Linear
                    sound.RollOffMinDistance = 10
                    sound.RollOffMaxDistance = 100
                    sound.Parent = hrp
                    sound:Play()

                    task.delay(10, function()
                        if sound then sound:Destroy() end
                    end)
                end
            else
                --------------------------------------------------
                -- OUTROS PLAYERS
                --------------------------------------------------

                -- 🔊 EXECUTA UMA VEZ
                if not tocouUmaVez then
                    tocouUmaVez = true
                    pcall(function()
                        SoundRemote:FireServer(hrp, tostring(ID), 5)
                    end)
                end

                -- 🌍 GLOBAL
                pcall(function()
                    local args = {
                        hrp, hrp,
                        Vector3.new(0,-1,0),
                        Vector3.new(0,0,0),
                        hrp, hrp,
                        tostring(ID), tostring(ID),
                        {true},
                        {25, Vector3.new(0.25,0.25,100), BrickColor.new(24), 0.25, Enum.Material.SmoothPlastic, 0.25},
                        true, false
                    }

                    GunRemote:FireServer(unpack(args))
                end)
            end

            task.wait(0.05)
        end
    end
end

--////////////////////////////////////////////////////
-- CONTROLES

Tab7:AddButton({
    Name = "▶ Tocar Som",
    Description = "Toca o áudio global",
    Callback = function()
        if InputID then
            PlayGlobal(InputID)
        end
    end
})

Tab7:AddToggle({
    Name = "🔁 Loop Global",
    Description = "Repetir áudio",
    Default = false,
    Callback = function(state)
        loopAtivo = state
        if state then
            task.spawn(function()
                while loopAtivo do
                    if InputID then
                        PlayGlobal(InputID)
                    end
                    task.wait(0.5)
                end
            end)
        end
    end
})

--////////////////////////////////////////////////////
-- DROPDOWN

local function createSoundDropdown(title, musicOptions, defaultOption)
    local musicNames = {}
    local map = {}

    for _, sounds in pairs(musicOptions) do
        for _, music in ipairs(sounds) do
            table.insert(musicNames, music.name)
            map[music.name] = music.id
        end
    end

    local selectedID = nil
    local loop = false

    Tab7:AddDropdown({
        Name = "🎵 "..title,
        Description = "Escolha um som",
        Default = defaultOption,
        Options = musicNames,
        Callback = function(opt)
            selectedID = map[opt]
        end
    })

    Tab7:AddButton({
        Name = "▶ Tocar Selecionado",
        Callback = function()
            if selectedID then
                PlayGlobal(selectedID)
            end
        end
    })

    Tab7:AddToggle({
        Name = "🔁 Loop Selecionado",
        Default = false,
        Callback = function(state)
            loop = state
            if state then
                task.spawn(function()
                    while loop do
                        if selectedID then
                            PlayGlobal(selectedID)
                        end
                        task.wait(0.5)
                    end
                end)
            end
        end
    })
end

--////////////////////////////////////////////////////
-- LISTA (exemplo reduzido)

createSoundDropdown("Memes", {
    ["Memes"] = {
{name = "Sukuna", id = "139666537620285"},
{name = "Windows", id = "125936118513893"},
{name = "Windows erro", id = "2661731024"},
{name = "Ahhhhh", id = "138326175698800"},
{name = "Faz o L", id = "133157583643691"},
{name = "Expansão de domínio br", id = "102876122309105"},
{name = "Gojo", id = "138283818946733"},
{name = "Risada 1", id = "136714652845401"},
{name = "Risada 2", id = "136195112064921"},
{name = "Risada zoio", id = "93280381776563"},
{name = "Risada 3", id = "78806465878205"},
{name = "Risada 4", id = "76038831505190"},
{name = "Pega Minha marreta", id = "116164900957098"},
{name = "Só isso", id = "97243810524027"},
{name = "N teve graça", id = "127133189032694"},
{name = "Escuro", id = "18870782376"},
{name = "Explosão", id = "117610888513112"},
{name = "Explosão 2", id = "101006595303605"},
{name = "Sus", id = "72012887327238"},
{name = "Vó copia o Satoru gojo", id = "140175808370920"},
{name = "tu e seco", id = "77058106224586"},
{name = "vou te ensinar a respeitar", id = "117685294896990"},
{name = "zumbi mini", id = "77774003812009"},
{name = "Brasil sil Sil", id = "123466896275673"},
{name = "ci fudeu meu amigo", id = "121320468979531"},
{name = "fala da o papo", id = "134699681156023"},
{name = "ahhhhhhhhh", id = "133065318200768"},
{name = "pega o Jack", id = "133582774056892"},
{name = "sou bom", id = "129259905468567"},
{name = "porque essa diferença?", id = "120370340698141"},
{name = "meus ataques", id = "126667346252984"},
{name = "você quer marretada do Tor?", id = "102037691397539"},
{name = "molestado pelo pai", id = "135334987359228"},
{name = "Makita", id = "131862097205793"},
{name = "ahhhhhhhhhhhhhh", id = "98660719604777"},
{name = "aí minha canela", id = "94187671110604"},
{name = "se levanta baiano", id = "129456656692974"},
{name = "eu sou o melhor", id = "82406273626578"},
{name = "aí meu cu", id = "91931550918967"},
{name = "pankapakan", id = "122547522269143"},
{name = "Gemido ultra rápido", id = "128863565301778"},
{name = "vai g0z@?", id = "116293771329297"},
{name = "nao choraxxx", id = "94377077452021"},
{name = "G0z33iiii", id = "93462644278510"},
{name = "Hommmm ", id = "133135656929513"},
{name = "gemido2", id = "92186909873950"},
{name = "sus sex", id = "128137573022197"},
{name = "Hentai wiaaaaan", id = "88332347208779"},
{name = "ai meu c*zinho slowed", id = "71895544093312"},
{name = "Loly gemiD0", id = "119277017538197"},
{name = "ai poison", id = "115870718113313"},
{name = "chegachega SUS", id = "77405864184828"},
{name = "uwu", id = "76820720070248"},
{name = "ai meu cuzin", id = "130714479795369"},
{name = "girl audio 2", id = "84207358477461"},
{name = "Hoo ze da manga", id = "106624090319571"},
{name = "ai alexandre de moraes", id = "107261471941570"},
{name = "haaii meme", id = "120006672159037"},
{name = "GoGogo gogogo", id = "103262503950995"},
{name = "Toma jack", id = "132603645477541"},
{name = "Toma jackV2", id = "100446887985203"},
{name = "Toma jack no sol quente", id = "97476487963273"},
{name = "ifood", id = "133843750864059"},
{name = "pelo geito ela ta querendo ram", id = "94395705857835"},
{name = "lula vai todo mundo", id = "136804576009416"},
{name = "coringa", id = "84663543883498"},
{name = "shoope", id = "8747441609"},
{name = "quenojo", id = "103440368630269"},
{name = "sai dai lava prato", id = "101232400175829"},
{name = "se e loko numconpeça", id = "78442476709262"},
{name = "mita sequer que eu too uma", id = "94889439372168"},
{name = "Deita aqui eu mandei vc deitar sirens", id = "100291188941582"},
{name = "miau", id = "131804436682424"},
{name = "skibidi", id = "128771670035179"},
{name = "BIRULEIBI", id = "121569761604968"},
{name = "biseabesjnjkasnakjsndjkafb", id = "133106998846260"},
{name = "vai corinthians!!....", id = "127012936767471"},
{name = "my sigman", id = "103431815659907"},
{name = "mama", id = "106850066985594"},
{name = "OH MY GOD", id = "73349649774476"},
{name = "aahhh plankton meme", id = "95982351322190"},
{name = "CHINABOY", id = "84403553163931"},
{name = "PASTOR MIRIM E A LÍNGUA DOS ANJOS", id = "71153532555470"},
{name = "Sai d3sgraç@", id = "106973692977609"},
{name = "opa salve tudo bem?", id = "80870678096428"},
{name = "OLHA O CARRO DO DANONE", id = "110493863773948"},
{name = "Nãoooo, Nãoooo, Nãoooo!!!!!", id = "95825536480898"},
{name = "UM PÉ DE SIRIGUELA KK", id = "112804043442210"},
{name = "e o carro da pamonha", id = "94951629392683"},
{name = "BOM DIAAAAAAAAAA", id = "136579844511260"},
{name = "ai-meu-chiclete", id = "92911732806153"},
{name = "posso te ligar ou tua mulher...", id = "103211341252816"},
{name = "Boa chi joga muito cara", id = "110707564387669"},
{name = "Oqueee meme", id = "120092799810101"},
{name = "kkk muito fei", id = "79241074803021"},
{name = "lula cade o ze gotinha", id = "86012585992725"},
{name = "morreu", id = "8872409975"},
{name = "a-pia-ta-cheia-de-louca", id = "98076927129047"},
{name = "Mahito killSong", id = "128669424001766"},
{name = "Sucumba", id = "7946300950"},
{name = "nem clicou o thurzin", id = "84428355313544"},
{name = "fiui OLHA MENSAGEM", id = "121668429878811"},
{name = "tooomeee", id = "128319664118768"},
{name = "risada de ladrao", id = "133065882609605"},
{name = "E o PIX nada ainda", id = "113831443375212"},
{name = "Vo nada vo nada", id = "89093085290586"},
{name = "Eli gosta", id = "105012436535315"},
{name = "um cavalo de tres pernas?", id = "8164241439"},
{name = "voces sao um bado de fdp", id = "8232773326"},
{name = "HAHA TROLLEI ATÉ VOCÊ", id = "7021794555"},
{name = "Calaboca Kenga", id = "86494561679259"},
{name = "alvincut", id = "88788640194373"},
{name = "e a risada faz como?", id = "140713372459057"},
{name = "voce deve se m@t4", id = "100227426848009"},
{name = "receba", id = "94142662616215"},
{name = "UUIIII", id = "73210569653520"},
{name = "sai", id = "121169949217007"},
{name = "risada boa dms", id = "127589011971759"},
{name = "vacilo perna de pau", id = "106809680656199"},
{name = "gomo gomo no!!!", id = "137067472449625"},
{name = "arroto", id = "140203378050178"},
{name = "iraaaa", id = "136752451575091"},
{name = "não fica se achando muito não", id = "101588606280167"},
{name = "WhatsApp notificaçãoV1", id = "107004225739474"},
{name = "WhatsApp notificaçãoV2", id = "18850631582"},
{name = "SamsungV1", id = "123767635061073"},
{name = "SamsungV2", id = "96579234730244"},
{name = "Shiiii", id = "120566727202986"},
{name = "ai_tomaa miku", id = "139770074770361"},
{name = "Miku Miku", id = "72812231495047"},
{name = "kuru_kuru", id = "122465710753374"},
{name = "PM ROCAM", id = "96161547081609"},
{name = "cavalo!!", id = "78871573440184"},
{name = "deixa os garoto brinca", id = "80291355054807"},
{name = "flamengo", id = "137774355552052"},
{name = "sai do mei satnas", id = "127944706557246"},
{name = "namoral agora e a hora", id = "120677947987369"},
{name = "n pode me chutar pq seu celebro e burro", id = "82284055473737"},
{name = "vc ta fudido vou te pegar", id = "120214772725166"},
{name = "deley", id = "102906880476838"},
{name = "Tu e um beta", id = "130233956349541"},
{name = "Porfavor n tira eu nao", id = "85321374020324"},
{name = "Olá beleza vc pode me dá muitos", id = "74235334504693"},
{name = "Discord sus", id = "122662798976905"},
{name = "rojao apito", id = "6549021381"},
{name = "off", id = "1778829098"},
{name = "Kazuma kazuma", id = "127954653962405"},
{name = "sometourado", id = "123592956882621"},
{name = "Estouradoespad", id = "136179020015211"},
{name = "Alaku bommm", id = "110796593805268"},
{name = "busss", id = "139841197791567"},
{name = "Estourado wItb", id = "137478052262430"},
{name = "sla", id = "116672405522828"},
{name = "HA HA HA", id = "138236682866721"}
    }
}, "Pankapakan")

--////////////////////////////////////////////////////
-- LOOP ESTOURADO

Tab7:AddToggle({
    Name = "💥 Loop Estourado",
    Description = "Som infinito forte",
    Default = false,
    Callback = function(state)
        loopSomFixo = state
        if state then
            task.spawn(function()
                while loopSomFixo do
                    PlayGlobal(somFixoID)
                    task.wait(0.8)
                end
            end)
        end
    end
})





local function tocarMusica(id)
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    
    -- Rádio (ToolMusicText)
    local argsRadio = {
        [1] = "ToolMusicText",
        [2] = id
    }
    ReplicatedStorage:WaitForChild("RE"):WaitForChild("PlayerToolEvent"):FireServer(unpack(argsRadio))
    
    -- Casa (PickHouseMusicText)
    local argsCasa = {
        [1] = "PickHouseMusicText",
        [2] = id
    }
    ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Player1sHous1e"):FireServer(unpack(argsCasa))

    -- Carro (PickingCarMusicText)
    local argsCarro = {
        [1] = "PickingCarMusicText",
        [2] = id
    }
    ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Player1sCa1r"):FireServer(unpack(argsCarro))

    -- Scooter (PickingScooterMusicText)
    local argsScooter = {
        [1] = "PickingScooterMusicText",
        [2] = id
    }
    ReplicatedStorage:WaitForChild("RE"):WaitForChild("1NoMoto1rVehicle1s"):FireServer(unpack(argsScooter))
end

local function isValidMusicId(value)
    return value and value ~= "" and value ~= "Option 1" and not value:match("novas musica adds") and not value:match("musica brasil") and not value:match("musica do meu interece") and not value:match("musica dls por elas") and not value:match("meme abaixo") and not value:match("estourada")
end

-- Caixa de texto para ID manual
Tab8:AddTextBox({
    Name = "ID da música",
    PlaceholderText = "Digite o ID e pressione Enter",
    Callback = function(value)
        if value and value ~= "" then
            tocarMusica(tostring(value))
        end
    end
})

-- Função para criar dropdowns de música automaticamente
local function createMusicDropdown(title, musicOptions, defaultOption)
    local musicNames = {}
    local categoryMap = {}
    for category, sounds in pairs(musicOptions) do
        for _, music in ipairs(sounds) do
            if music.name ~= "" then
                table.insert(musicNames, music.name)
                categoryMap[music.name] = {id = music.id, category = category}
            end
        end
    end

    local function playMusic(soundId)
        tocarMusica(tostring(soundId))
    end

    Tab8:AddDropdown({
        Name = title,
        Description = "all",
        Default = defaultOption,
        Multi = false,
        Options = musicNames,
        Callback = function(selectedSound)
            if selectedSound and categoryMap[selectedSound] then
                local soundId = categoryMap[selectedSound].id
                if soundId and soundId ~= "" and soundId ~= "4354908569" then
                    playMusic(soundId)
                end
            end
        end
    })
end

-- ==================== DROPDOWN FORRÓ ====================
createMusicDropdown("Forró", {
    ["forro"] = {
        {name = "forró já cansou", id = "74812784884330"},
        {name = "lembro até hoje", id = "71531533552899"},
        {name = "escolha certa", id = "107088620814881"},
        {name = "forró da resenha", id = "120973520531216"},
        {name = "forró dudu", id = "74404168179733"},
        {name = "forró são joão", id = "106364874935196"},
        {name = "forró engraçado paia", id = "76524290482399"},
        {name = "100% forro vaquejada", id = "92295159623916"},
        {name = "PASTOR MIRIM E A LÍNGUA DOS ANJOS", id = "71153532555470"},
        {name = "PARA NÃO ESQUECER QUEM SOMOS", id = "88937498361674"},
        {name = "Uno zero", id = "112959083808887"},
        {name = "Iate do neymar", id = "135738534706063"},
        {name = "Batidao na aldeia", id = "79953696595578"},
    }
}, "Option 1")

-- ==================== DROPDOWN MEMES ====================
createMusicDropdown("músicas e memes aleatórios", {
    ["Memes"] = {
        {name = "ANXIETY (Amapiano Re-fix)", id = "101483901475189"},  
        {name = "old town", id = "112211013080592"},
        {name = "Eminem", id = "128367264297998"},
        {name = "Weeks & 3 days", id = "120817494107898"},
        {name = "Megalovania but its only the melodies", id = "104500091160463"},
        {name = "androphono strikes back", id = "78312089943968"},
        {name = "Bamm Bamm", id = "128730685516895"},
        {name = "chupa cabra", id = "132890273173295"},
        {name = "longe demais", id = "124478512057763"},
        {name = "Garoto de Copacabana", id = "135648634110254"},
        {name = "CELL!", id = "117634275895085"},
        {name = "Boa vibe em Ubatuba", id = "139059061493558"},
        {name = "SLIP AWAY", id = "126152928520174"},
        {name = "Alone in Motion", id = "122379348696948"},
        {name = "Fade Away", id = "81002139735874"},
        {name = "Wounds & Wishes", id = "109347979566607"},
        {name = "Ascensão do Monarca", id = "101864243033211"},
        {name = "carro do ovo", id = "3148329638"},
        {name = "ingles bus (fling ou kill bus)", id = "123268013026823"},
        {name = "MIKU MIKU HATSUNE", id = "112783541496955"},
        {name = "Five Nights at Freddy's", id = "110733765539890"},
        {name = "Rat Dance", id = "133496635668044"},
        {name = "Escalando a Seleção Brasileira para a Copa", id = "116546457407236"},
    }
}, "Option 1")

-- ==================== DROPDOWN FUNK (CORRIGIDO) ====================
createMusicDropdown("Funk", {
    ["Funk"] = {
        {name = "Medley das drogas", id = "126941748428319"},
        {name = " BLACK LANÇA", id = "97471613998368"},
        {name = "zn x bdo", id = "92524227941055"},
        {name = "Juliana que que tu quer de mim!?", id = "77428616866753"},
        {name = "Tudo cheiroso", id = "115892145725960"},
        {name = "Agudo mágico", id = "132827213686172"},
        {name = "Madagascaralho", id = "77253110633591"},
        {name = "Funk mandelão/mandela: ", id = "96264520897761"},       
        {name = "Medley das drogas", id = "126941748428319"},
        {name = "APERTA MARCHA", id = "108226178439659"},
        {name = "Chuva De Arroz", id = "102120036739514"},
        {name = "Piui Tic Tac - MC RD", id = "128599225685461"},
        {name = "Pega sente o som do tapa", id = "97708834121472"},
        {name = "vem socando", id = "114436245577405"},
        {name = "o japonêsinha", id = "73508063540527"},
        {name = "Senta tua GXSTXSX", id = "110176270788146"},
        {name = "Famosinha { Estourado}", id = "112406825739796"},
        {name = "OFF THE KING", id = "120324849313242"},
        {name = "amo", id = "96259351729049"},
        {name = "Então desisti", id = "102878660090014"},
        {name = "Hoje eu vou pro baile trajardão de Nike", id = "107416893652681"},
        {name = "meu IPHONE", id = "103288558732219"},
        {name = "082 assombra", id = "139969588022082"},
        {name = "SEU EX TA NERVOSO DEMAIS", id = "125175056369746"},
        {name = "Me desbroqueia papai", id = "84571646803645"},
        {name = "ela e fogosa vem sentando", id = "76079350135381"},
        {name = "Baile dos antares", id = "16331041475"},
        {name = "CHARLIE KIRKY", id = "79910195620356"},
        {name = "Vai deitar sem sono", id = "128219511199109"},
        {name = "Novinha no grau", id = "100511855558684"},
        {name = "BROTA NA BASE", id = "72970505446502"},
        {name = "bota ela pra sentar", id = "133679572515204"},
        {name = "índio", id = "133318423431098"},
        {name = "sperma", id = "73144366170218"},
        {name = "fantasma", id = "76699533647710"},
        {name = "Macporra", id = "102325227710789"},
        {name = "eu n sou gay", id = "85523458332774"},
        {name = "meia noite", id = "82117652303865"},
        {name = "união flasco", id = "81628621752701"},
        {name = "dom dom dom", id = "101882948595303"},
        {name = "tipo nino", id = "140336018782383"},
        {name = "nino pt2", id = "97708834121472"},
        {name = "puro lança", id = "128441181589899"},
        {name = "dança do canguru", id = "86876136192157"},
        {name = "melodia", id = "139147474886402"},
        {name = "cappuccino assassino", id = "132733033157915"},
        {name = "bonde do Discord", id = "79581762870032"},
        {name = "bitch", id = "103288558732219"},
        {name = "rebola", id = "121046655523341"},
        {name = "posso até não te dar flores", id = "107708637749079"},
        {name = "Savana Teto", id = "135327809788134"},
        {name = "bota aqui na base", id = "72970505446502"},
        {name = "Aí que saudade do cherin do Block blashe", id = "124638151165836"},
        {name = "Tiger roubadona", id = "125329520501218"},
        {name = "Beat Malvado DJ OBL", id = "83174160244345"},
        {name = "Automotivo do Pou 2.0 DJ NK3", id = "104826703821683"},
        {name = "Recebi uma mensagem da sua amiguinha MC Lan", id = "130637458480604"},
        {name = "O meu carro bixo, motel de motor", id = "135005299702467"},
        {name = "O cheiro bom é o de BCT", id = "117476956542254"},
        {name = "Amor é mentira DJ Arana", id = "78544797215218"},
        {name = "Eu já sofri por amor", id = "128011871344522"},
        {name = "Na brisa uma japonesinha", id = "134130716324734"},
        {name = "MC Tuto Barbie", id = "134879044098791"},
        {name = "Pocket Renk do Halloween", id = "94600803570382"},
        {name = "Onda Subaquática", id = "75422081790794"},
        {name = "na onda berimbau", id = "140530013819453"},
        {name = "DJ Das Favela", id = "131489289833086"},
        {name = "raspa o cabelo dela", id = "106866829236727"},
        {name = "bata essa ####### aqui", id = "99250885177756"},
        {name = "mico", id = "78352220341424"},
        {name = "violen", id = "104767744632555"},
        {name = "Em Bora", id = "131847084942844"},
        {name = "Pancadão", id = "76312991186384"},
        {name = "Vovo ta casado", id = "103336395794625"},
        {name = "Meno", id = "124899151072996"},
        {name = "Naruto", id = "89473100926016"},
        {name = "Querendo", id = "110877994245293"},
        {name = "Ei Moça", id = "138830667342178"},
        {name = "Empina na Onda", id = "104621031886653"},
        {name = "berimbau", id = "77712236704085"},
        {name = "Bonde da peixaria", id = "135355953783002"},
        {name = "deixa o portão aberto", id = "125323304808942"},
        {name = "funk da hora", id = "106958630419629"},
        {name = "funk pesadão", id = "72720721570850"},
        {name = "mtg funk", id = "81384105684889"},
        {name = "vapo vapo", id = "135903820233276"},
        {name = "pesadelo", id = "82016192638562"},
        {name = "ponto", id = "130758596227702"},
        {name = "fino", id = "107481584214750"},
        {name = "boneco", id = "122747553795687"},
        {name = "Seu Fã", id = "85342086082111"},
        {name = "Grelô", id = "72200166265935"},
        {name = "Senta", id = "124085422276732"},
        {name = "Zum Zum Zum", id = "92446612272052"},
        {name = "sombria hahaha", id = "72855385972818"},
        {name = "Quem é o Louco", id = "106958630419629"},
        {name = "coração", id = "102705292327383"},
        {name = "Rave Favela", id = "127013133418864"},
        {name = "V7", id = "80348640826643"},
        {name = "Se Apaixona", id = "135662066137645"},
        {name = "melodia virtual", id = "139147474886402"},
        {name = "pixel", id = "93928823862203"},
        {name = "viagem sonora", id = "79349174602261"},
        {name = "sequência", id = "121187736532042"},
        {name = "Fuga Na Viatura", id = "131891110268352"},
        {name = "Penélope", id = "97011217688307"},
        {name = "eu já tava bem", id = "93590122047380"},
        {name = "Oruan - Sou Teu fã", id = "85342086082111"},
        {name = "Grelo So fé", id = "72200166265935"},
        {name = "Melodia Funk", id = "83064304128519"},
        {name = "Poze Cv", id = "124244582950595"},
        {name = "beats", id = "16190782181"},
        {name = "Soldado Guerreiro", id = "71590664026646"},
        {name = "bao", id = "89789578077519"},
        {name = "Uniao Flasco", id = "100989716917693"},
        {name = "DO PO", id = "114207745067816"},
    }
}, "Option 1")

-- ==================== DROPDOWN PHONK (CORRIGIDO) ====================
createMusicDropdown("Phonk", {
    ["phonk"] = {
        {name = "Montagem Espanca Loli", id = "98623641908787"},
        {name = "Montagem os 3 macete", id = "138730944430517"},
        {name = "Sócrates", id = "135526249310486"},
        {name = "Aquela Sister", id = "110919391228823"},
        {name = "MTG INSURDECEDORA", id = "125354466627612"},
        {name = "mtg tu vai sentar 2", id = "87701232785766"},
        {name = "mtg toma toma", id = "137254194634453"},       
        {name = "MONTAGEM AUTOMOTIVA DO FUTURO", id = "87299594714360"},
        {name = "MONTAGEM VEM F*D*R NA ONDA DA LOLO", id = "79919454273891"},
        {name = "Automotivo new", id = "135721378392049"},
        {name = "MONTAGEM SANTA", id = "132597601384537"},
        {name = "Montagem soca na danada", id = "100337717591420"},
        {name = "Montagem Emirados Árabe", id = "98412934676146"},
        {name = "Montagem Espanca Mente 2", id = "135729578171550"},
        {name = "Montagem estoura tímpano", id = "113624200970506"},
        {name = "Montagem da uma baforada", id = "76983583262607"},
        {name = "Montagem quebra cama", id = "104976012074245"},
        {name = "Montagem Terror dos 7 mares DJ OBC", id = "101658851913310"},
        {name = "Montagem Mini game", id = "85014780788991"},
        {name = "Montagem berimbau sequencial", id = "71619650167212"},
        {name = "Montagem submundo GH7", id = "137107451922636"},
        {name = "Montagem Abravanel", id = "78614982249626"},
        {name = "Montagem beat panelado", id = "105438850576001"},
        {name = "entre clxy", id = "110274202819990"},
        {name = "montagem encanta", id = "74258284867725"},
        {name = "montagem Speed", id = "120342836549924"},
        {name = "montagem Nova geração", id = "119293698405737"},
        {name = "montagem senta senta", id = "83656625240780"},
        {name = "montagem boa não sei o nome", id = "105663768150465"},
        {name = "montagem risada sombria", id = "93556552625864"},
        {name = "montagem do karma", id = "79148752172118"},
        {name = "entre", id = "110274202819990"},
        {name = "kobal", id = "79381341943021"},
        {name = "estourada", id = "92175624643620"},
        {name = "estourada 2", id = "108099943758978"},
        {name = "montagem no Money", id = "133498664318216"},
        {name = "montagem emote", id = "121803455304591"},
        {name = "vamo agressiva", id = "82680101995105"},
        {name = "perdi o nome", id = "113494161114548"},
        {name = "mtg phonk", id = "77148331286853"},
        {name = "potencial", id = "102333287500691"},
        {name = "passo bem solto", id = "127388462601694"},
        {name = "montagem diamante", id = "113208690604605"},
        {name = "montagem lualua", id = "759649143831591"},
        {name = "montagem ma", id = "98337901681441"},
        {name = "montagem relacionamento", id = "113072044238343"},
        {name = "montagem você sabe a diferença?", id = "107260053679799"},
        {name = "montagem um pouco mais", id = "81665575945119"},
        {name = "montagem link 30!", id = "120189561947098"},
        {name = "montagem enlouquecer", id = "121703297529684"},
        {name = "montagem bailão", id = "129071695986627"},
        {name = "tomagi mo tão", id = "111668097052966"},
        {name = "Batalla de bombas", id = "71995582569283"},
        {name = "Another hiit", id = "99731755429975"},
        {name = "Want To Love", id = "104846670980072"},
        {name = "Onichan", id = "17422156627"},
        {name = "gol contra", id = "122114766584918"},
        {name = "Trying To finde", id = "132643763204246"},
        {name = "Montagem Sinistra Dz7", id = "78098390943739"},
        {name = "CRISTAL", id = "103445348511856"},
    }
}, "Option 1")

-- Botão para parar todas as músicas
Tab8:AddButton({
    Name = "stop músic",
    Description = "ALL music",
    Callback = function()
        tocarMusica("")
    end
})






--// ===============================
--// CONFIG RGB
--// ===============================
local rgbSpeed = 3

Tab9:AddSlider({
    Name = "Velocidade RGB",
    Description = "Aumenta a velocidade do efeito RGB",
    Min = 1,
    Max = 10,
    Increase = 1,
    Default = 3,
    Callback = function(Value)
        rgbSpeed = Value
    end
})

--// ===============================
--// FUNÃ‡Ã•ES
--// ===============================
local function getRainbowColor(speedMultiplier)
    local h = (tick() * speedMultiplier % 10) / 10
    return Color3.fromHSV(h, 1, 1)
end

local function fireServer(eventName, args)
    local RE = ReplicatedStorage:FindFirstChild("RE")
    if RE and RE:FindFirstChild(eventName) then
        pcall(function()
            RE[eventName]:FireServer(unpack(args))
        end)
    end
end

--// ===============================
--// SEÃ‡ÃƒO NOME / BIO
--// ===============================
Tab9:AddSection({ "RGB para usar em vocÃª" })

local nameBioRGBActive = false
Tab9:AddToggle({
    Name = "Nome + Bio RGB",
    Default = false,
    Callback = function(state)
        nameBioRGBActive = state
        if state then
            task.spawn(function()
                while nameBioRGBActive do
                    local color = getRainbowColor(rgbSpeed)

                    fireServer("1RPNam1eColo1r", { "PickingRPNameColor", color })
                    fireServer("1RPNam1eColo1r", { "PickingRPBioColor", color })

                    task.wait(0.03)
                end
            end)
        end
    end
})

--// ===============================
--// RGB CABELO
--// ===============================
local ToggleCabelo = Tab9:AddToggle({
    Name = "RGB Cabelo",
    Description = "RGB no cabelo",
    Default = false
})

ToggleCabelo:Callback(function(Value)

    getgenv().rgbCabelo = Value

    task.spawn(function()

        while getgenv().rgbCabelo do

            fireServer("1Max1y", {
                "ChangeHairColor2",
                getRainbowColor(rgbSpeed)
            })

            task.wait(0.5)

        end

    end)

end)

--// CONTROLE
local rgbAtivo = false
local rgbThread

--// LISTA DE CORES
local cores = {
    "Light reddish violet","Salmon","Sunrise","Carnation pink","Pink","Hot pink",
    "Really red","Tawny","Burgundy","Maroon","Cocoa","Rust","CGA brown","Beige",
    "Brick yellow","Burlap","Fawn brown","Br. yellowish orange","Olive","New Yeller",
    "Br. yellowish green","Mint","Artichoke","Lime green","Grime","Forest green",
    "Parsley green","Earth green","Slime green","Toothpaste","Pastel light blue",
    "Cyan","Bright blue","Really blue","Dark blue","Navy blue","Royal purple",
    "Magenta","Bright violet","Eggplant","Mulberry","Institutional white",
    "Mid gray","Cloudy grey","Flint","Fossil","Medium stone grey",
    "Dark stone grey","Smoky grey","Black","Really black","Dirt brown",
    "Pine Cone","Brown","Dark nougat","Light orange","Pastel brown"
}

--// LOOP RGB
local function iniciarRGB()

    if rgbThread then return end

    rgbThread = task.spawn(function()

        while rgbAtivo do

            for _, cor in ipairs(cores) do

                if not rgbAtivo then break end

                pcall(function()
                    ReplicatedStorage.Remotes.ChangeBodyColor:FireServer(cor)
                end)

                task.wait(0.02) -- EXTREMAMENTE RÃPIDO

            end

        end

        rgbThread = nil

    end)

end

--// TOGGLE RGB
Tab9:AddToggle({
    Name = "RGB Corpo",
    Default = false,
    Callback = function(v)

        rgbAtivo = v

        if v then
            iniciarRGB()
        end

    end
})






--// ===============================
--// VEÃCULOS E CASA
--// ===============================
Tab9:AddSection({ "VeÃ­culos e Casa" })

local ToggleCasa = Tab9:AddToggle({
    Name = "RGB Casa",
    Description = "RGB na casa",
    Default = false
})

ToggleCasa:Callback(function(Value)

    getgenv().rgbCasa = Value

    task.spawn(function()

        while getgenv().rgbCasa do

            fireServer("1Player1sHous1e", {
                "ColorPickHouse",
                getRainbowColor(rgbSpeed)
            })

            task.wait(0.1)

        end

    end)

end)

local carRGBActive = false

Tab9:AddToggle({
    Name = "Carro RGB (Premium)",
    Description = "RGB contÃ­nuo no carro (pode kickar se nÃ£o for premium)",
    Default = false,

    Callback = function(state)

        carRGBActive = state

        if state then

            task.spawn(function()

                while carRGBActive do

                    fireServer("1Player1sCa1r", {
                        "PickingCarColor",
                        getRainbowColor(rgbSpeed)
                    })

                    task.wait(0.03)

                end

            end)

        end

    end
})


--// RGB VEÃCULOS SEM MOTOR
getgenv().rgbNoMotor = false

Tab9:AddToggle({
    Name = "RGB VeÃ­culos Sem Motor",
    Description = "Skate, Bicicleta, Scooter, etc",
    Default = false,

    Callback = function(Value)

        getgenv().rgbNoMotor = Value

        if Value then

            task.spawn(function()

                while getgenv().rgbNoMotor do

                    fireServer("1Player1sCa1r", {
                        "NoMotorColor",
                        getRainbowColor(rgbSpeed)
                    })

                    task.wait(0.08)

                end

            end)

        end

    end
})


--// RGB CAVALO
getgenv().rgbHorse = false

Tab9:AddToggle({
    Name = "RGB Cavalo",
    Description = "Troca a cor do cavalo a cada 0.9s",
    Default = false,

    Callback = function(Value)

        getgenv().rgbHorse = Value

        if Value then

            task.spawn(function()

                while getgenv().rgbHorse do

                    -- Cor 1
                    pcall(function()
                        ReplicatedStorage.RE["1Hors1eRemot1e"]:FireServer("DefaultHorse")
                    end)

                    task.wait(0.9)

                    if not getgenv().rgbHorse then break end

                    -- Cor 2
                    pcall(function()
                        ReplicatedStorage.RE["1Hors1eRemot1e"]:FireServer("SecondaryHorse")
                    end)

                    task.wait(0.9)

                end

            end)

        end

    end
})



--// ===============================
--// ITENS / TOOLS
--// ===============================
Tab9:AddSection({ "Itens / Tools" })

local radioRGBActive = false

Tab9:AddToggle({
    Name = "RÃ¡dio RGB",
    Description = "RGB contÃ­nuo no rÃ¡dio",
    Default = false,

    Callback = function(state)

        radioRGBActive = state

        if state then

            task.spawn(function()

                while radioRGBActive do

                    local color = getRainbowColor(rgbSpeed)

                    local success, remote = pcall(function()
                        return LocalPlayer.PlayerGui.ToolGui.ToolSettings.Settings.PropsColor.SetColor
                    end)

                    if success and remote then
                        remote:FireServer(color)
                    end

                    task.wait(0.03)

                end

            end)

        end

    end
})


local function toolRGB(toggleVar)

    task.spawn(function()

        while getgenv()[toggleVar] do

            local color = getRainbowColor(rgbSpeed)

            local gui = LocalPlayer:FindFirstChild("PlayerGui")

            if gui and gui:FindFirstChild("ToolGui") then

                local props = gui.ToolGui.ToolSettings.Settings.PropsColor

                if props and props:FindFirstChild("SetColor") then

                    pcall(function()
                        props.SetColor:FireServer(color)
                    end)

                end

            end

            task.wait(0.1)

        end

    end)

end


Tab9:AddToggle({
    Name = "RGB Megafone",
    Default = false,

    Callback = function(Value)

        getgenv().rgbMegafone = Value
        toolRGB("rgbMegafone")

    end
})


Tab9:AddToggle({
    Name = "RGB Rosquinha",
    Default = false,

    Callback = function(Value)

        getgenv().rgbRosquinha = Value
        toolRGB("rgbRosquinha")

    end
})


print("âœ… Aba RGB carregada com sucesso")







local savedDoors = {}

Tab12:AddToggle({
    Name = "Atravessar Doors",
    Default = false,
    Callback = function(Value)

        if Value then
            -- ATIVAR
            for _, obj in pairs(workspace:GetDescendants()) do
                if obj:IsA("BasePart") then
                    local name = string.lower(obj.Name)

                    if name == "door" or name == "doors" then
                        if not savedDoors[obj] then
                            savedDoors[obj] = {
                                Transparency = obj.Transparency,
                                CanCollide = obj.CanCollide
                            }
                        end

                        obj.Transparency = 1 -- invisível
                        obj.CanCollide = false -- atravessar
                    end
                end
            end

        else
            -- DESATIVAR (voltar ao normal)
            for obj, props in pairs(savedDoors) do
                if obj and obj.Parent then
                    obj.Transparency = props.Transparency
                    obj.CanCollide = props.CanCollide
                end
            end

            savedDoors = {}
        end
    end
})









local folder = workspace:WaitForChild("Vehicles")

local AntiVehiclesList = {
    "car","vehicle", "carr", "carro", "jat", "jato", "veicolos", "boat pirat","Helicopter","Helicóptero", "Motorcycle", "Plane", "Airplane", "Truck", "Ship", "Pickup Truck", "Bus", "Canoa", "Canoe", "Speedboat", "boat"
}

local saved = {}
local conn
local ativo = false

local function isTarget(name)
    name = string.lower(name)
    for _,v in pairs(AntiVehiclesList) do
        if string.find(name, v) then
            return true
        end
    end
    return false
end

local function applyVehicle(vehicle)
    for _,obj in ipairs(vehicle:GetDescendants()) do
        if obj:IsA("BasePart") then

            if not saved[obj] then
                saved[obj] = {
                    Anchored = obj.Anchored,
                    CanCollide = obj.CanCollide
                }
            end

            obj.Anchored = true
            obj.CanCollide = false
        end
    end
end

local function restore()
    for obj,props in pairs(saved) do
        if obj and obj.Parent then
            obj.Anchored = props.Anchored
            obj.CanCollide = props.CanCollide
        end
    end
    saved = {}
end

Tab12:AddToggle({
    Name = "Anti Vehicles ",
    Default = false,
    Callback = function(state)
        ativo = state

        if state then
            -- veículos já existentes
            for _,v in ipairs(folder:GetChildren()) do
                applyVehicle(v)
            end

            -- veículos novos (ESSA PARTE É A CERTA)
            conn = folder.ChildAdded:Connect(function(v)
                if not ativo then return end
                task.wait(0.2)
                applyVehicle(v)
            end)

        else
            if conn then
                conn:Disconnect()
                conn = nil
            end

            restore()
        end
    end
})





local Players = game:GetService("Players")
local plr = Players.LocalPlayer

local antiSit = false
local charConn
local loop

local function startAntiSit(char)
    local humanoid = char:WaitForChild("Humanoid")

    -- loop leve (garante 100%)
    loop = task.spawn(function()
        while antiSit and humanoid and humanoid.Parent do
            
            -- se sentar ou tentar sentar
            if humanoid.Sit or humanoid.SeatPart then
                humanoid.Sit = false
                humanoid.Jump = true
                humanoid:ChangeState(Enum.HumanoidStateType.Running)
            end

            task.wait(0.1) -- leve, não trava
        end
    end)
end

Tab12:AddToggle({
    Name = "Anti Sent",
    Default = false,
    Callback = function(state)
        antiSit = state

        if state then
            -- personagem atual
            if plr.Character then
                startAntiSit(plr.Character)
            end

            -- após morrer/resetar
            charConn = plr.CharacterAdded:Connect(function(char)
                task.wait(0.2)
                if antiSit then
                    startAntiSit(char)
                end
            end)

        else
            -- desligar
            antiSit = false

            if charConn then
                charConn:Disconnect()
                charConn = nil
            end
        end
    end
})



local targetName = "BigGymWeight"

local removidos = {}
local conn
local ativo = false

local function removeObj(obj)
    if not removidos[obj] then
        removidos[obj] = obj.Parent
        obj.Parent = nil -- remove sem deletar
    end
end

local function restoreAll()
    for obj, parent in pairs(removidos) do
        if obj then
            obj.Parent = parent
        end
    end
    removidos = {}
end

local function check(obj)
    if string.find(string.lower(obj.Name), string.lower(targetName)) then
        removeObj(obj)
    end
end

Tab12:AddToggle({
    Name = "Anti Bloco",
    Default = false,
    Callback = function(state)
        ativo = state

        if state then
            -- remover existentes
            for _,obj in ipairs(workspace:GetDescendants()) do
                check(obj)
            end

            -- remover novos
            conn = workspace.DescendantAdded:Connect(function(obj)
                if not ativo then return end
                task.defer(function()
                    check(obj)
                end)
            end)

        else
            if conn then
                conn:Disconnect()
                conn = nil
            end

            restoreAll()
        end
    end
})




local Players = game:GetService("Players")
local plr = Players.LocalPlayer

local ativo = false
local loop
local charConn

local posHistory = {}

local function start(char)
    local hrp = char:WaitForChild("HumanoidRootPart")

    posHistory = {}

    loop = task.spawn(function()
        while ativo and hrp and hrp.Parent do
            
            -- salvar posição com tempo
            table.insert(posHistory, {
                pos = hrp.Position,
                time = tick()
            })

            -- limpar histórico antigo (mais de 5s)
            for i = #posHistory, 1, -1 do
                if tick() - posHistory[i].time > 5 then
                    table.remove(posHistory, i)
                end
            end

            local y = hrp.Position.Y

            -- 🔻 caiu no void leve
            if y < -300 then
                for i = #posHistory, 1, -1 do
                    if tick() - posHistory[i].time >= 3 then
                        hrp.CFrame = CFrame.new(posHistory[i].pos)
                        break
                    end
                end
            end

            -- 💀 caiu MUITO (fail safe)
            if y < -460 then
                hrp.CFrame = CFrame.new(hrp.Position.X, 100, hrp.Position.Z)
            end

            task.wait(0.1)
        end
    end)
end

Tab12:AddToggle({
    Name = "Anti Void",
    Default = false,
    Callback = function(state)
        ativo = state

        if state then
            -- personagem atual
            if plr.Character then
                start(plr.Character)
            end

            -- após morrer/resetar
            charConn = plr.CharacterAdded:Connect(function(char)
                task.wait(0.2)
                if ativo then
                    start(char)
                end
            end)

        else
            ativo = false

            if charConn then
                charConn:Disconnect()
                charConn = nil
            end
        end
    end
})
